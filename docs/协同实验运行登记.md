# 协同实验运行登记

> 目的：多台服务器并行推进仿真时，共享已跑实验、失败原因、结果路径和下一步任务，避免重复运行或覆盖结果。

## 协作规则

1. 每台服务器开始工作前先阅读本文件、`AGENTS.md` 和最近的交接文档。
2. 每个实验使用唯一 suite/log 名，建议包含服务器或任务标识，例如 `*_server_a`、`*_server_b`。
3. 不直接覆盖其他服务器生成的 `logs/<suite>`；需要重跑时新建后缀，如 `_rerun400u`。
4. PPO 与非 PPO 分开登记。PPO 结果必须记录是否达到 `target_updates`。
5. 论文结论优先使用 `seeds=1 2 3` 的正式批次；单 seed 只作为 smoke 或初步对比。

## 已知服务器分工

| 服务器 | 工作目录 | 建议分支 | 当前角色 |
|---|---|---|---|
| server-a | `/root/OMNET/SpatialReuse-TDMA` | `server-a/<task>` | 新增 paper-inspired baseline、MANET smoke、PPO 400-update 补跑 |
| server-b | `/home/opp_env/DynamicTDMA/DynamicTDMA` | `server-b/<task>` | 用户报告已有训练/正式实验推进，等待其提交同步 |

## 已完成或已知实验

| 日期 | 服务器 | suite/log root | 场景 | 方法 | 状态 | 论文用途 |
|---|---|---|---|---|---|---|
| 2026-05-10 | server-a | `logs/benchmark_paper_baseline_manet_smoke` | `N9_ring_manet_pedestrian`、`N12_grid_manet_pedestrian`、`N16_clustered_manet_sparse` | `plain_tdma`、`heuristic_only`、`greedy_stdma`、`traffic_adaptive_tdma`、`zmac_like`、`trama_like`、`bandit_index_proxy` | 21/21 完成；确认 `mobility_mode=random_waypoint` | MANET 非 PPO 初步对比，单 seed |
| 2026-05-10 | server-a | `logs/benchmark_paper_ppo_manet_compare` | 同上 | `ppo_baseline`、`B` | `N9` 达到 400 updates；`N12/N16` 因 `sim_time=15000s` 只到 344/279 updates | 仅 `N9` 可按 400 updates 使用；`N12/N16` 只作失败记录 |
| 2026-05-10 | server-a | `logs/benchmark_paper_ppo_manet_compare_400u` | `N12_grid_manet_pedestrian`、`N16_clustered_manet_sparse` | `ppo_baseline`、`B` | 4/4 完成，均达到 `target_updates=400` | MANET PPO 初步对比，单 seed |
| 2026-05-10 | server-a | `logs/benchmark_paper_baseline_tiny` | `N9_ring` | `zmac_like`、`trama_like`、`bandit_index_proxy` | 完成，但为静态拓扑 | 只作连通性验证，不作为 MANET 结论 |
| 2026-05-10 | server-b | `logs/benchmark_manet_formal_compare_v3_20260510_151910` | 未在 server-a 本地验证 | 未知 | 用户转述：不要重复跑主表实验 | 等待 server-b 提交登记和结果摘要 |

## 当前注意事项

- `scripts/benchmark_suite.sh` 已修复：`omnet_only` 分支现在会透传 `SCENARIO_MOBILITY_ARGS`。没有该修复的旧非 PPO `_manet` 批次不应作为 MANET 结果。
- `logs/benchmark_paper_ppo_manet_compare` 中 `N12/N16` 虽然 summary 显示 `完整run=1/1`，但训练没有达到 400 updates；正式引用应使用 `logs/benchmark_paper_ppo_manet_compare_400u`。
- OMNeT++ 末尾 `undisposed object` 警告仍存在，是已有清理问题；目前不影响 exit code 和指标文件生成。

## 下一步建议

1. server-b 提交其 `manet_formal_compare_v3` 的登记、summary 表和关键命令。
2. server-a/server-b 分工扩展正式批次：一边跑 `seeds=1 2 3` 的非 PPO MANET，一边跑 PPO 400-update 或更长训练。
3. 所有新增实验结束后，把本文件和对应交接文档随代码一起提交。
