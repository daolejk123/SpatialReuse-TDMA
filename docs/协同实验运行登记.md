# 协同实验运行登记

> 用途：多台服务器或多个 agent 异步运行仿真时，统一记录任务、命令、结果目录、完整性检查和论文可用性。
> 规则：任何正式实验、smoke、附录实验、敏感性实验完成后都应更新本文件。大型 `logs/`、`checkpoints/`、`out/` 不直接提交 Git。

## 状态枚举

- `planned`：已计划，尚未运行。
- `running`：正在运行。
- `completed`：运行完成，结果可分析，但尚未认定进入论文。
- `invalid`：参数、代码版本、训练预算或完整性不满足要求，不进入论文。
- `imported`：从另一台服务器导入，尚未复核。
- `accepted_for_paper`：已复核，可作为论文主表或附录候选数据。

## 协作规则

1. 每台服务器开始工作前先阅读本文件、`AGENTS.md` 和最近的交接文档。
2. 每个实验使用唯一 suite/log 名，建议包含服务器或任务标识，例如 `*_server_a`、`*_server_b`。
3. 不直接覆盖其他服务器生成的 `logs/<suite>`；需要重跑时新建后缀，如 `_rerun400u`。
4. PPO 与非 PPO 分开登记。PPO 结果必须记录是否达到 `target_updates`。
5. 论文结论优先使用 `seeds=1 2 3` 的正式批次；单 seed 只作为 smoke 或初步对比。

## 已知服务器分工

> **2026-05-10 起改为单服务器路线**：本机 `local-codex` 接手所有任务，不再使用双服务器并行流程。下表保留为历史记录，新条目"服务器/Agent"列填 `local-codex`。

| 服务器 | 工作目录 | 建议分支 | 当前角色 |
|---|---|---|---|
| local-codex（**当前**） | `/home/opp_env/DynamicTDMA/DynamicTDMA` | 任意特性分支 | 全部任务：算法改进、主表实验、附录扩展、文献基线对比、文档维护 |
| ~~server-a~~（历史） | `/root/OMNET/SpatialReuse-TDMA` | `server-a/<task>` | 新增 paper-inspired baseline、MANET smoke、PPO 400-update 补跑 |
| ~~server-b~~（历史） | `/home/opp_env/DynamicTDMA/DynamicTDMA` | `server-b/<task>` | MANET 正式主表、UAV-inspired/FANET 稀疏压力场景、引用/图表后续 |

## 当前基准结果

| 任务ID | 服务器/Agent | 分支/Commit | Suite/结果目录 | 场景 | 方法 | 预算 | 状态 | 完整性检查 | 论文用途 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| manet-formal-v3 | server-b/local-codex | `origin/master@8506d88` 后续以最终合并 commit 为准 | `logs/benchmark_manet_formal_compare_v3_20260510_151910` | `N12_grid_manet_pedestrian`, `N12_grid_manet_uav_sparse` | `plain_tdma`, `heuristic_only`, `greedy_stdma`, `traffic_adaptive_tdma`, `ppo_baseline`, `B` | `seeds=1 2 3`, `sim_time=20000`, `target_updates=400`, `jobs=2`, `metrics_mode=summary` | `accepted_for_paper` | 36/36 `run_status.tsv`; 12/12 PPO/B 达到 `target_updates reached: 400`; 非 PPO 无 `python.log`; `omnetpp.ini` 无残留 diff | MANET 主表候选 | `sim_time=15000` 的早期正式尝试无效，v3 是当前有效正式数据 |
| server-a-paper-baseline-manet-smoke | server-a/codex | `server-a/manet-baselines-and-registry` | `logs/benchmark_paper_baseline_manet_smoke` | `N9_ring_manet_pedestrian`, `N12_grid_manet_pedestrian`, `N16_clustered_manet_sparse` | `plain_tdma`, `heuristic_only`, `greedy_stdma`, `traffic_adaptive_tdma`, `zmac_like`, `trama_like`, `bandit_index_proxy` | `seeds=1`, `sim_time=300`, `jobs=2`, `metrics_mode=summary` | `completed` | 21/21 完成；运行配置确认 `mobility_mode=random_waypoint` | MANET 非 PPO 初步对比 | 单 seed smoke，不作为正式论文结论 |
| server-a-ppo-manet-n9 | server-a/codex | `server-a/manet-baselines-and-registry` | `logs/benchmark_paper_ppo_manet_compare` | `N9_ring_manet_pedestrian` | `ppo_baseline`, `B` | `seeds=1`, `target_updates=400`, 默认 `sim_time=15000` | `completed` | 2/2 达到 `target_updates reached: 400` | MANET PPO 初步对比 | 单 seed；N12/N16 同目录未达 400 updates，见下一行 |
| server-a-ppo-manet-15000-invalid | server-a/codex | `server-a/manet-baselines-and-registry` | `logs/benchmark_paper_ppo_manet_compare` | `N12_grid_manet_pedestrian`, `N16_clustered_manet_sparse` | `ppo_baseline`, `B` | `seeds=1`, `target_updates=400`, `sim_time=15000` | `invalid` | N12 仅到 344/400 updates；N16 仅到 279/400 updates | 不用于论文 | summary 有 `完整run=1/1` 但训练预算不足 |
| server-a-ppo-manet-400u | server-a/codex | `server-a/manet-baselines-and-registry` | `logs/benchmark_paper_ppo_manet_compare_400u` | `N12_grid_manet_pedestrian`, `N16_clustered_manet_sparse` | `ppo_baseline`, `B` | `seeds=1`, `sim_time=25000`, `target_updates=400`, `jobs=2` | `completed` | 4/4 达到 `target_updates reached: 400` | MANET PPO 初步对比 | 单 seed；用于替代上一条 invalid 的 N12/N16 结果 |
| server-a-paper-baseline-tiny | server-a/codex | `server-a/manet-baselines-and-registry` | `logs/benchmark_paper_baseline_tiny` | `N9_ring` | `zmac_like`, `trama_like`, `bandit_index_proxy` | `seeds=1`, `sim_time=3` | `completed` | 3/3 完成 | 连通性验证 | 静态拓扑，不作为 MANET 结论 |
| manet-formal-v1-invalid | server-b/local-codex | 待记录最终 commit | `logs/benchmark_manet_formal_compare_20260510_111355` | 同 v3 | 同 v3 | `sim_time=15000`, `target_updates=400` | `invalid` | PPO/B 仅到 344/400 updates | 不用于论文 | 只保留为调试记录 |
| manet-formal-v2-invalid | server-b/local-codex | 待记录最终 commit | `logs/benchmark_manet_formal_compare_v2_20260510_122355` | 同 v3 | 同 v3 | `sim_time=20000`, `target_updates=400` | `invalid` | 中途断掉，只留下部分 run | 不用于论文 | 不混入正式结果 |

## 待分配任务池

| 任务ID | 建议服务器/Agent | 推荐 suite 名 | 场景 | 方法 | 预算 | 状态 | 目标 | 备注 |
|---|---|---|---|---|---|---|---|---|
| uav-sparse-comm200-smoke | server-b-codex | `manet_sensitivity_comm200_server_b_smoke` | `N12_grid_manet_uav_sparse` 或新增 `_manet_uav_sparse200` | 6 方法全量 | `seeds=1`, `sim_time=6000`, `target_updates=100`, `jobs=2` | `planned` | 验证 `comm_range=200` 是否适合附录敏感性实验 | 若新增场景后缀，先静态检查脚本；不要替代 v3 主表 |
| manet-v3-figures | server-b-codex | 无需跑仿真 | `benchmark_manet_formal_compare_v3_20260510_151910` | 读取汇总 CSV | 无 | `planned` | 生成 PDR、goodput/slot、Wt_P95、queue_slope 图表 | 图注必须说明是 2D MANET/FANET abstraction |
| server-a-paper-baselines-seeds3 | server-a-codex | `paper_baseline_manet_seeds3_server_a` | 可与 v3 主表对齐 | `zmac_like`, `trama_like`, `bandit_index_proxy` 等新增文献基线 | `seeds=1 2 3`, 非 PPO `sim_time>=300` | `planned` | 扩展 server-a 新增基线到 3 seeds | 避免重复 v3 已完成 6 方法主表 |
| server-a-paper-baseline-appendix | server-a-codex | `paper_baseline_manet_appendix_server_a` | `N12_grid_manet_pedestrian`, `N12_grid_manet_uav_sparse` | `zmac_like`, `trama_like`, `bandit_index_proxy` | `seeds=1 2 3`, `sim_time=20000`, `jobs=2` | `planned` | 与 v3 主表场景对齐，补充 paper-inspired 非学习基线附录 | PR #1 合并后由 server-a 执行；不替代 v3 主表 |
| manet-sparse200-smoke-server-b | server-b-codex | `server-b/manet-sparse200-appendix@83d6c18` | `logs/benchmark_manet_sparse200_smoke_server_b_20260510_202848` | `N12_grid_manet_uav_sparse200` | v3 6 方法 | `seeds=1`, `sim_time=6000`, `target_updates=100`, `jobs=2` | `completed` | 6/6 `run_status.tsv`; 2/2 PPO/B 达到 `target_updates reached: 100`; 非 PPO 无 `python.log`; `comm_range=200m` 已在日志确认 | smoke，可支撑启动正式附录 | 新增 `_manet_uav_sparse200` 后缀；未替代 v3 主表 |
| manet-sparse200-appendix-server-b | server-b-codex | `server-b/manet-sparse200-appendix@83d6c18` | `logs/benchmark_manet_sparse200_appendix_server_b_20260510_203258` | `N12_grid_manet_uav_sparse200` | v3 6 方法 | `seeds=1 2 3`, `sim_time=20000`, `target_updates=400`, `jobs=2` | `completed` | 18/18 `run_status.tsv`; 6/6 PPO/B 达到 `target_updates reached: 400`; 非 PPO 无 `python.log`; `omnetpp.ini` 无残留 diff | UAV-inspired sparse `comm_range=200` 敏感性附录候选 | 结果显示 `comm_range=200` 下非学习 `greedy_stdma/heuristic_only` PDR 仍高，PPO/B MAC 效率和尾延迟较好 |
| starvation-penalty-smoke | local-codex | 待提交 commit | `logs/smoke_starvation_penalty` | `N12_grid_default`（ini N=9，--num_nodes 9） | `ppo_baseline`（隐含，run_joint.sh 默认） | `seeds=N/A`, `sim_time=6000`, `target_updates=100`, `jobs=1` | `completed` | `target_updates reached: 100`; entropy=0.6922 末段（远高于 0.3）; L_critic 0.93~0.96; 无 OMNeT++ 异常 | 代码可用性验证，**不进论文** | 改进 19 饥饿惩罚的 smoke；超参 `--starvation_threshold 5 --starvation_penalty_coef 0.1` |
| manet-sparse200-starvation-fix | local-codex | `758a521` | `logs/benchmark_manet_sparse200_starvation_fix_20260511_014614` | `N12_grid_manet_uav_sparse200` | `ppo_baseline`, `B` | `seeds=1 2 3`, `sim_time=20000`, `target_updates=400`, `jobs=2`, `--starvation_threshold 5 --starvation_penalty_coef 0.1` | `completed` | 6/6 run_status.tsv；6/6 PPO 达 `target_updates reached: 400`；无错误 | 饥饿惩罚有效性验证 | starvation B −70.7%、ppo −75%（双双优于 heuristic 0.47）；PDR B +1.4%、ppo +22.9%（**意外修复 distribution shift**）；Wt_P95 双双 −9.8% |
| manet-formal-compare-v4 | local-codex | `758a521` | `logs/benchmark_manet_formal_compare_v4_20260511_104010` | `N12_grid_manet_pedestrian`, `N12_grid_manet_uav_sparse` | `plain_tdma`, `heuristic_only`, `greedy_stdma`, `traffic_adaptive_tdma`, `ppo_baseline`, `B` | `seeds=1 2 3`, `sim_time=20000`, `target_updates=400`, `jobs=2`, 启用饥饿惩罚 | `completed` | 36/36 run_status.tsv；12/12 PPO/B 达 `target_updates reached: 400`；非 PPO 无 `python.log`；未发现 run_status 失败项 | 主表候选 / starvation-fix 对照 | v4 对 sparse/UAV 的 B starvation 修复明显，但 pedestrian 吞吐/PDR 回退；不直接标 accepted_for_paper，v3 继续保留为当前主表候选 |
| paper-baseline-manet-appendix-local | local-codex | `758a521` | `logs/benchmark_paper_baseline_manet_appendix_local_20260511_014617` | `N12_grid_manet_pedestrian`, `N12_grid_manet_uav_sparse` | `zmac_like`, `trama_like`, `bandit_index_proxy` | `seeds=1 2 3`, `sim_time=20000`, `jobs=2`, `metrics_mode=summary` | `completed` | 18/18 run_status.tsv；非 PPO 无 python.log（符合预期）；无错误 | 论文文献基线对比附录 | paper-inspired 基线整体弱于 v3 非学习基线，但补完对比矩阵足够 |
| thesis-baseline-smoke | local-codex | 待提交 commit | `logs/benchmark_thesis_baseline_smoke_20260514_010338`; `logs/smoke_zmac_inspired_20260514`; `logs/smoke_trama_inspired_20260514` | `N12_grid_manet_pedestrian` / `N12_grid` | `greedy_stdma_2hop`, `zmac_inspired`, `trama_inspired` | `greedy_stdma_2hop: seeds=1 2 3, sim_time=300`; aliases: `sim_time=30` | `completed` | `greedy_stdma_2hop` 3/3 完成并生成 summary；`zmac_inspired`/`trama_inspired` 30s run_joint 正常 exit 0 | 入口验证，**不进论文主结论** | 论文 baseline 强化 smoke；正式论文表需另跑 v3 两主表场景 × 3 seeds |

## 新实验登记模板

新增实验时复制下面一行到对应表格：

| 任务ID | 服务器/Agent | 分支/Commit | Suite/结果目录 | 场景 | 方法 | 预算 | 状态 | 完整性检查 | 论文用途 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| `<task-id>` | `<server-or-agent>` | `<branch>@<commit>` | `<logs/benchmark_...>` | `<scenarios>` | `<methods>` | `<seeds/sim_time/target_updates/jobs/metrics>` | `planned` | `<run_status/python.log/target_updates checks>` | `<main/appendix/smoke/invalid>` | `<notes>` |

## 标准完整性检查命令

把 `<RESULT_DIR>` 替换为实际结果目录：

```bash
find <RESULT_DIR> -name run_status.tsv | wc -l
find <RESULT_DIR> -name python.log | wc -l
find <RESULT_DIR> -name python.log -exec grep -H "target_updates reached: 400" {} \; | wc -l
find <RESULT_DIR> -path '*/python.log' | rg -v '/(ppo_baseline|B)/' || true
git diff -- omnetpp.ini
```

正式 MANET 6 方法 × 2 场景 × 3 seeds 的预期：

```text
run_status.tsv = 36
python.log = 12
target_updates reached: 400 = 12
非 PPO python.log = 空
omnetpp.ini diff = 空
```

## 当前注意事项

- `scripts/benchmark_suite.sh` 已修复：`omnet_only` 分支现在会透传 `SCENARIO_MOBILITY_ARGS`。没有该修复的旧非 PPO `_manet` 批次不应作为 MANET 结果。
- server-a 的 `logs/benchmark_paper_ppo_manet_compare` 中 `N12/N16` 虽然 summary 显示 `完整run=1/1`，但训练没有达到 400 updates；正式引用应使用 `logs/benchmark_paper_ppo_manet_compare_400u`。
- server-b 的 `manet-formal-v3` 是当前登记为 `accepted_for_paper` 的正式主表候选；不要重复跑同一 suite。
- OMNeT++ 末尾 `undisposed object` 警告仍存在，是已有清理问题；目前不影响 exit code 和指标文件生成。
