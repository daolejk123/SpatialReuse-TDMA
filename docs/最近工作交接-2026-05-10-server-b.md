# 最近工作交接（给另一台服务器 Codex）

> 时间：2026-05-10  
> 适用对象：另一台服务器上的 Codex / Claude / 实验执行 agent  
> 目的：在不实时通信的情况下协同推进 DynamicTDMA 的 MANET 论文实验、分析和后续敏感性实验。

## 先读这些文件

接手前请先阅读：

- `AGENTS.md`
- `docs/最近工作交接-2026-05-10.md`
- `docs/算法改进记录.md`
- `docs/论文指标与性能对比基准.md`

如果本文件与更晚日期的交接文档冲突，以更晚日期的交接文档为准。

## 当前有效实验状态

当前已经完成一轮可作为论文主表候选的正式 MANET 对比实验：

- 有效结果目录：`logs/benchmark_manet_formal_compare_v3_20260510_151910`
- 场景：
  - `N12_grid_manet_pedestrian`
  - `N12_grid_manet_uav_sparse`
- 方法：
  - `plain_tdma`
  - `heuristic_only`
  - `greedy_stdma`
  - `traffic_adaptive_tdma`
  - `ppo_baseline`
  - `B`
- 预算：
  - `seeds=1 2 3`
  - `sim_time=20000`
  - `target_updates=400`
  - `metrics_mode=summary`
- 完整性：
  - 36/36 run 完成
  - 12/12 PPO/B 达到 `target_updates reached: 400`
  - 非 PPO 目录无 `python.log`

注意：以下目录不作为正式论文数据：

- `logs/benchmark_manet_formal_compare_20260510_111355`：PPO/B 只到 344/400 updates。
- `logs/benchmark_manet_formal_compare_v2_20260510_122355`：中途断掉，只留下部分 run。

正式主表优先使用：

```text
logs/benchmark_manet_formal_compare_v3_20260510_151910
```

## 协作规则

- 请使用独立 Git 分支，不要直接在主工作分支上改。
  - 推荐分支名：`server-b/<task-name>`
- 不要回滚或覆盖另一台服务器已有改动。
- 不要使用 `git add -A` 或 `git add .`。
- 不要提交大型 `logs/`、`checkpoints/`、`out/`。
- 如果运行实验，请使用唯一 suite 名，避免覆盖已有结果：
  - `manet_sensitivity_comm200_server_b`
  - `formal_appendix_n15_server_b`
- 每轮实验完成后必须记录：
  - 当前 commit
  - 运行命令
  - 结果目录
  - 完整性检查
  - 是否可进入论文主表、附录或仅作 smoke

## 可以继续推进的任务

### 1. 结果分析与论文表格

优先读取：

```text
logs/benchmark_manet_formal_compare_v3_20260510_151910/summaries/comparison_table.md
logs/benchmark_manet_formal_compare_v3_20260510_151910/summaries/metrics_scenario_method_summary.csv
```

建议整理中文论文表述：

- 普通 MANET：`B` 和 `ppo_baseline` 优于非学习基线。
- 稀疏 UAV-inspired：`heuristic_only` / `greedy_stdma` 的 PDR 更高，PPO/B 的 MAC 效率较高，`B` 的 Wt_P95 较低。
- 不要宣称 RL 在所有场景、所有指标上全面最优。

### 2. 附录或敏感性实验

可以考虑只跑小规模 sensitivity，不要直接替代正式主表。

推荐方向：

- `N12_grid_manet_uav_sparse` with `comm_range=200`
- 或新增 `_manet_uav_sparse200` 场景；改代码前先确认是否已有类似后缀。

推荐先 smoke：

```bash
source /home/opp_env/omnetpp-6.3.0/setenv && \
./scripts/benchmark_suite.sh \
  --suite manet_sensitivity_comm200_server_b_smoke \
  --scenarios "N12_grid_manet_uav_sparse" \
  --methods "plain_tdma heuristic_only greedy_stdma traffic_adaptive_tdma ppo_baseline B" \
  --seeds "1" \
  --sim_time 6000 \
  --target_updates 100 \
  --jobs 2 \
  --metrics_mode summary \
  --save_every 5000 \
  --stale_timeout 600
```

如果 smoke 有意义，再扩展到正式预算；不要在没有 smoke 的情况下直接跑大规模实验。

### 3. 画图与统计

基于 `metrics_scenario_method_summary.csv` 生成：

- PDR 柱状图
- goodput/slot 柱状图
- Wt_P95 对比图
- queue_slope 对比图

图和表应标注场景是 2D MANET/FANET abstraction，不是完整 3D UAV 物理层。

## 必须保留的论文口径

`_manet_uav_sparse` 应表述为：

```text
2D UAV-inspired sparse high-mobility MANET/FANET pressure scenario
```

不要声称实现了：

- 3D 高度
- LOS/NLOS
- 路径损耗
- 能耗模型
- 完整 UAV/FANET 物理层

`benchmark_manet_smoke_20260510_100014` 的非 PPO MANET 结果不适合作为正式对比，因为早期 `omnet_only` 没有传递 mobility 参数。

## 启动前建议执行

```bash
git status --short
bash -n scripts/benchmark_suite.sh scripts/ablation.sh scripts/run_joint.sh
find logs/benchmark_manet_formal_compare_v3_20260510_151910 -name run_status.tsv | wc -l
find logs/benchmark_manet_formal_compare_v3_20260510_151910 -name python.log | wc -l
find logs/benchmark_manet_formal_compare_v3_20260510_151910 -name python.log -exec grep -H "target_updates reached: 400" {} \; | wc -l
```

预期：

```text
run_status.tsv = 36
python.log = 12
target_updates reached: 400 = 12
```

## 交付格式

完成任何任务后，请新增或更新一份交接文档：

```text
docs/最近工作交接-YYYY-MM-DD-server-b.md
```

内容至少包括：

- 做了什么
- 修改了哪些文件
- 跑了哪些命令
- 结果目录
- 哪些结果可用于论文
- 哪些结果无效或只作参考
- 下一步建议
