# 最近工作交接 - 2026-05-10 - server-a

> 本文档记录 `/root/OMNET/SpatialReuse-TDMA` 上的最新工作，供另一台服务器或后续 Codex 接手前阅读。

## 当前代码状态

- 新增并跑通 3 个 paper-inspired 非学习 baseline：
  - `zmac_like`
  - `trama_like`
  - `bandit_index_proxy`
- 修复 `scripts/benchmark_suite.sh`：`omnet_only` 分支现在会透传 MANET mobility 参数。没有这个修复时，`*_manet_*` 场景名可能没有真正启用 `random_waypoint`。
- 已更新实验说明文档：
  - `docs/算法改进记录.md`
  - `docs/论文指标与性能对比基准.md`
  - `docs/协同实验运行登记.md`
- 当前本地还有未提交改动，开始新任务前请先看 `git status --short`。

## 已完成实验

### 非 PPO 真 MANET baseline

结果目录：

```text
logs/benchmark_paper_baseline_manet_smoke
```

命令口径：

```bash
bash scripts/benchmark_suite.sh \
  --suite paper_baseline_manet_smoke \
  --scenarios "N9_ring_manet_pedestrian N12_grid_manet_pedestrian N16_clustered_manet_sparse" \
  --methods "plain_tdma heuristic_only greedy_stdma traffic_adaptive_tdma zmac_like trama_like bandit_index_proxy" \
  --seeds "1" --sim_time 300 --jobs 2 \
  --metrics_mode summary --sim_log_mode file \
  --root_log logs/benchmark_paper_baseline_manet_smoke
```

结论状态：21/21 完成，属于单 seed 初步 MANET 对比。

### PPO MANET 对比

第一轮目录：

```text
logs/benchmark_paper_ppo_manet_compare
```

其中 `N9_ring_manet_pedestrian` 的 `ppo_baseline` 和 `B` 达到 `target_updates=400`。但 `N12_grid_manet_pedestrian` 和 `N16_clustered_manet_sparse` 在默认 `sim_time=15000s` 下没有达到 400 updates，不应作为正式 400-update 结果。

补跑目录：

```text
logs/benchmark_paper_ppo_manet_compare_400u
```

补跑命令口径：

```bash
bash scripts/benchmark_suite.sh \
  --suite paper_ppo_manet_compare_400u \
  --scenarios "N12_grid_manet_pedestrian N16_clustered_manet_sparse" \
  --methods "ppo_baseline B" \
  --seeds "1" --sim_time 25000 --target_updates 400 --jobs 2 \
  --metrics_mode summary --sim_log_mode file \
  --root_log logs/benchmark_paper_ppo_manet_compare_400u
```

结论状态：4/4 达到 `target_updates=400`。

## 不要重复跑

- 不要重复跑 `logs/benchmark_paper_baseline_manet_smoke`，除非要扩展 seeds 或修改参数。
- 不要把 `logs/benchmark_paper_ppo_manet_compare` 的 `N12/N16` 当作 400-update 正式结果。
- 用户转述 server-b 已有 `logs/benchmark_manet_formal_compare_v3_20260510_151910` 主表实验，另一台服务器应提交登记后再决定是否补跑。

## 建议下一步

1. 先把本地代码和文档改动提交到专用分支，例如 `server-a/manet-baselines-and-registry`。
2. 让 server-b 也提交其正式主表实验登记，尤其是命令、场景、方法、seeds、summary 表和是否达到 PPO 训练目标。
3. 合并后再设计正式 `seeds=1 2 3` 批次，避免两边重复跑同一 suite。
