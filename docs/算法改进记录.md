# 算法改进记录

记录 PPO 闭环训练过程中观察到的问题、分析及改进方案。

---

## 问题 1：奖励崩溃（策略退化）

**发现时间**：2026-03-15 闭环训练首次验证

**现象**：
训练初期 `avg_r` 约 +44（与启发式策略持平），但随训练推进迅速下降至负值：

```
frame=   32  avg_r=+44.367  entropy=0.6926
frame=  352  avg_r=+47.134  entropy=0.6886   ← 短暂上升
frame=  960  avg_r=+28.230  entropy=0.5076   ← 开始下降
frame= 1184  avg_r=+0.457   entropy=0.4229
frame= 1664  avg_r=-91.629  entropy=0.3823   ← 严重崩溃
frame= 1920  avg_r=-197.371 entropy=0.3038
```

**分析**：
1. **熵坍缩**：`entropy` 从 0.693（均匀随机）快速降至 0.30，策略过早收敛到确定性动作
2. **协作失败**：多节点独立学习同一个策略网络（共享参数），容易收敛到"所有节点选同一时隙"的局部最优，导致大量冲突
3. `ent_coef=0.01` 过小，不足以维持探索

**可能的改进方向**：

### 方向 A：增大熵正则化
```python
# 当前
ent_coef = 0.01

# 建议尝试
ent_coef = 0.05  # 或 0.1
```
熵正则化系数决定了策略"愿意保持多大随机性"。增大后策略会更慢收敛，但可避免过早陷入差的局部最优。

### 方向 B：降低学习率
```python
# 当前
lr = 3e-4

# 建议尝试
lr = 1e-4  # 更保守的更新步长
```

### 方向 C：增加 GAE（广义优势估计）
当前 advantage 计算是简单的单步 TD：`A_t = r_t + γ·V(t+1) - V(t)`。引入 GAE(λ) 可以减小方差：
```python
# RolloutBuffer.compute_advantages() 中替换为 GAE
gae = 0.0
for t in reversed(range(len(traj))):
    delta = traj[t].reward + gamma * next_val - traj[t].value
    gae = delta + gamma * gae_lambda * gae
    traj[t].advantage = gae
    traj[t].ret = gae + traj[t].value
```

### 方向 D：节点 ID 嵌入（区分各节点策略）
当前所有节点共享同一组参数且输入中没有节点 ID 信息，导致策略无法区分不同节点。可在状态向量中加入 one-hot 节点 ID 编码或可学习嵌入，使不同节点能学习差异化策略。

---

## 问题 4：ActionSender 连接延迟（frame 3072 才生效）

**发现时间**：2026-03-29 第一次完整闭环验证

**现象**：
```
[ActionSender] 动作管道暂未连接（C++ 仿真未启动），将延迟重试
...（3072 帧后）...
[ActionSender] 动作管道已连接
```
前 3072 帧 C++ 使用启发式策略，RL 网络在"启发式分布"上学习。
闭环一生效，分布发生偏移，avg_r 从 +89 开始缓慢下降，最终在 frame 4800 崩溃。

**分析**：
- Python 先于仿真启动，`ActionSender.open()` 以 O_NONBLOCK 尝试打开动作管道写端
- 此时 C++ 尚未打开读端 → ENXIO 失败，`_fd = None`
- C++ 在 `initialize()` 中打开 action 管道读端，但 Python 仅在 `send()` 时重试连接
- `send()` 每帧调用一次，重试逻辑在 send() 内部，最终在某帧连接成功

**核心问题**：前期训练数据由启发式策略生成，策略网络学习了错误的状态-价值映射，闭环后发生 off-policy 偏差。

**修复方案**（2026-03-29）：
- 在 `rl_receiver.py` 的 `connect()` 中尽早创建 ActionSender 实例并尝试连接
- C++ 端在 `initialize()` 阶段就打开 action 管道读端，确保 Python 首次 `send()` 时能立即连接
- 修复后 RL 动作从 frame 1 开始生效，消除前期 off-policy 偏差

---

## 问题 5：L_critic 全程偏高（300-1000+）

**发现时间**：2026-03-29 同次验证

**现象**：
```
frame=   64  L_critic=372.6581
frame= 1920  L_critic=358.2158
frame= 4864  L_critic=382.4927  ← 崩溃开始后急剧升高
frame= 5696  L_critic=1058.4805
```

**分析**：
- avg_r 约 +89，returns 约为同量级（假设 γ=0.99，长期 return ≈ r/(1-γ) = 8900）
- Critic 的 MSE Loss = (V - R)² 量级自然很大
- 缺少奖励归一化（reward normalization），导致 Critic 难以收敛

**修复方案**（2026-03-29）：
在 `ppo_update()` 中对 returns 进行归一化，与 advantages 处理方式一致：
```python
# 在 ppo_update() 中，advantages 已归一化，对 returns 也做归一化
returns = (returns - returns.mean()) / (returns.std() + 1e-8)
```

修复后 L_critic 从 300-1000+ 降至 1-10 量级，Critic 收敛速度显著提升。

---

## 问题 2：前 50 帧无 RL 动作（冷启动延迟）

**现象**：
C++ 从 frame 53 才开始收到 RL 动作，前 52 帧使用启发式策略。

**分析**：
- Python 先启动 → `ActionSender.open()` 因 C++ 尚未启动而失败
- C++ 启动后打开 action 管道读端
- Python 在第一帧 `send()` 中重连成功
- 但 RL 动作基于上一帧观测，所以实际上从 frame 2 就应该有动作
- 实际延迟约 50 帧，可能由 OMNeT++ 仿真速度快于 Python 处理速度导致

**影响**：轻微。前 50 帧使用启发式策略，对长期训练影响不大。

**可能的改进**：C++ 端在写完 state 管道后可短暂等待（如 1ms），给 Python 处理时间。但这会降低仿真速度，需权衡。

---

## 问题 3：（待观察）on-policy 纯度

**潜在问题**：
PPO 是 on-policy 算法，要求训练数据来自当前策略。但由于管道异步性和 C++ 仿真速度，可能出现：
- C++ 使用的是 frame N 的 RL 动作
- Python 训练使用的是 frame N+K 的观测（K 帧延迟）

这会引入 off-policy 偏差，可能影响训练稳定性。

**可能的改进**：
- 引入同步机制（C++ 等待 Python 返回动作后再继续）
- 或者切换到 off-policy 算法（如 SAC），天然容忍数据延迟

---

## 超参数参考表

| 参数 | 当前值 | 说明 | 调优建议 |
|:--|:--|:--|:--|
| `lr` | 3e-4 | 学习率 | 若奖励不稳定，降至 1e-4 |
| `ent_coef` | **0.05** | 熵正则化权重（学习阶段） | **自适应调整**：新增 ent_coef_high=0.10，当 entropy 低于 0.45 时自动切换 |
| `ent_coef_high` | **0.10** | 熵正则化权重（稳定阶段） | 当 entropy < entropy_adapt_low 时生效，防止崩溃 |
| `entropy_adapt_high` | 0.55 | 自适应切换起点（线性过渡） | entropy > 0.55 使用 ent_coef；entropy < 0.45 使用 ent_coef_high |
| `entropy_adapt_low` | 0.45 | 自适应切换终点（完全稳定） | 低于此值立即使用 ent_coef_high |
| `clip_eps` | 0.2 | PPO 裁剪范围 | 通常不需要改 |
| `vf_coef` | 0.5 | Critic 损失权重 | 若 L_critic 过大可降至 0.25 |
| `update_every` | 32 | 每 K 帧更新 | 增大到 64 可提升稳定性 |
| `ppo_epochs` | **2** | 每次更新迭代数 | 4→2 防止过拟合（2026-04-11 调整） |
| `gamma` | 0.99 | 折扣因子 | 若关注短期可降至 0.95 |
| `max_grad_norm` | 0.5 | 梯度裁剪 | 通常不需要改 |
| `r_alpha` | 1.0 | 吞吐量奖励权重 | - |
| `r_beta` | 1.0 | 冲突惩罚权重 | 已增至 1.0（2026-03-29） |
| `r_gamma` | **0.5** | 公平性奖励权重 | 0.3→0.5 强化公平性激励（2026-04-11 调整） |
| `r_delta` | 0.2 | 时延惩罚权重 | - |

---

## 改进实验日志

> 在此记录每次调参/改动的实验结果。

| 日期 | 改动 | 训练帧数 | 最终 avg_r | 备注 |
|:--|:--|:--|:--|:--|
| 2026-03-15 | 基线（默认参数） | 1920 | -197 | 奖励崩溃，熵过快下降 |
| 2026-03-29 | 节点 ID one-hot + GAE(λ=0.95) + ent_coef=0.05 + r_beta=1.0 | 6528 | -25（frame 6400） | **奖励崩溃延至 frame 4800（延长 3 倍）**，熵 0.47+（vs 0.30），前 4800 帧 avg_r 维持 +82~+96 |
| 2026-03-29 | 修复 ActionSender 延迟连接 + L_critic 归一化 | - | - | RL 动作从 frame 1 生效，L_critic 从 300-1000+ 降至 1-10 量级 |
| 2026-04-11 | ent_coef 0.05→0.1 + ppo_epochs 4→2 + r_gamma 0.3→0.5 + 从 frame4000 checkpoint 重训 | 357 帧（11 次更新） | +47.9 ~ +53.2 | avg_r 较崩溃后 +43 提升约 15%；entropy 缓慢衰减 0.690→0.678（ent_coef=0.1 生效）；Tx 成功率 60.4%（↑vs 59.3%）；Jain 0.607（↑vs 0.591）；Wt 0.62s（↓vs 0.68s） |
| 2026-04-11 | 同上参数 + 学习率指数衰减（lr_decay_gamma=0.9995） + sim-time-limit 500s | 1760 帧（55 次更新） | **+47~+55（稳定，无崩溃）** | **关键突破**：首次在 frame 1760 时 avg_r 仍正值（历史 ent=0.05 在 frame 1920 崩溃至 -197）。entropy 衰减速率在减速（0.0048→0.0042/update）。Jain 0.622（↑），Wt 0.577s（↓）。entropy@1760=0.337，距危险阈值 0.30 约 9 次更新 |
| 2026-04-11 | ent_coef 0.1→0.15 + 其余不变，从 1760 帧 checkpoint 继续 | 1760 帧（55 次更新） | +50→+36（缓慢下滑） | **结论：ent_coef 须与策略当前熵值匹配**。从低熵权重（entropy=0.337）上施加大 ent_coef=0.15，导致 actor 与 entropy 梯度冲突，策略混乱下滑（非崩溃）。entropy 最终降至 0.179，avg_r 末段滑至 +34~+36 |
| 2026-04-11 | 从零初始化重训（ent_coef=0.10） | 1760 帧 | **+44~+52（无进展）** | **负面结论**：ent_coef=0.10 配合随机初始化过强。entropy 几乎不动（0.692→0.671），L_actor≈0，策略停在随机动作阶段无法收敛。actor 梯度被 entropy 梯度完全压制 |
| 2026-04-11 | Phase1（ent_coef=0.05, ppo_epochs=2, 从零开始） | 1760f（55次更新） | **+44~+52（稳定，无崩溃）** | entropy: 0.692→0.537。L_actor 前25次更新≈0（无学习），后30次更新0.02~0.10（有学习）。avg_r 全程平坦，**与随机基线（+49）无显著提升**。ent_coef=0.05+ppo_epochs=2 = 熵正则与actor梯度相互制约，策略无法突破随机基线 |
| 2026-04-11 | Phase1b（从Phase1 checkpoint加载，ppo_epochs=4, r_gamma=0.3） | 736f | avg_r: +39→+15（**崩溃**） | entropy: 0.537→0.344 → 崩溃。**关键发现：collapse 阈值 ≈ entropy 0.34，与 ent_coef 无关**。不论 0.05 还是 0.10，只要 entropy 降至 0.34 且 ent_coef 不够强，即崩溃 |
| 2026-04-11 | Phase2（加载Phase1b崩溃checkpoint entropy=0.18，ent_coef=0.10） | 1760f | avg_r: +20→-1600（**灾难性崩溃**） | 加载了被Phase1b覆盖的已崩溃checkpoint（entropy=0.18）。教训：不可从极低熵checkpoint继续训练 |
| 2026-04-11 | **核心发现**：历史 +82~+96 重新评估 | - | 历史数据修正 | **历史 +82~+96 为启发式策略性能，非RL学习结果**（ActionSender fix前，RL在frame 3072才接管，前3072帧为C++启发式策略输出）。RL 从零学习的实际上限为 +44~+52，与随机策略（+49）无显著差异 |
| 2026-04-11 | 自适应ent_coef（从零，ent_coef=0.05→0.10 at entropy 0.55→0.45） | 1760f（55次更新） | **+44~+52（稳定）** | entropy: 0.692→0.655（慢于Phase1，随机种子差异）。ent=0.050全程未触发自适应（entropy仍高于0.55）。需更多帧才能进入切换区 |
| 2026-04-11 | 自适应ent_coef（加载frame7000 checkpoint，entropy初始≈0.43→自动用ent_coef_high=0.10） | 1760f | avg_r: +48→-33（**崩溃**） | 加载frame7000后entropy=0.43，ent_coef_high=0.10生效。但entropy仍降至0.217，avg_r崩溃。**根本原因**：从entropy=0.43开始，actor梯度仍强于ent_coef=0.10。KEY BREAKTHROUGH稳定在entropy=0.337是特殊均衡点，无法从更高熵值收敛至此 |
| 2026-04-11 | **架构改进**：实现自适应ent_coef机制（ppo_trainer.py） | - | - | 新增字段：ent_coef_high, entropy_adapt_high, entropy_adapt_low。新增逻辑：按entropy线性插值自动调整ent_coef。新增修复：加载checkpoint时初始_last_entropy=0（触发ent_coef_high），避免第一次更新使用低ent_coef破坏均衡 |

---

## 问题 6：通信性能指标系统评估（2026-04-11）

**发现时间**：2026-04-11 通信性能日志检测

### 测量方法

仿真结束后分析 `results/` 目录下三类 CSV（本次时间戳 `20260411_222713`，71 帧）：
- `slot_stats_*.csv`：累计请求数 / 成功传输数 / 实际包数
- `frame_metrics_*.csv`：逐帧每节点 Wt（队头等待）、Qt（队长）、Cctrl（控制冲突）、Jlocal（局部公平性）
- `fairness_*.csv`：全局 Jain 指数逐帧趋势

### 性能数据（Apr 11 vs Mar 29 对比）

| 指标 | Apr 11（RL 崩溃后） | Mar 29（RL 高峰期） | 目标 |
|:--|:--|:--|:--|
| 全网 Tx 成功率 | **59.3%** | 50.9% | >70% |
| 平均 Jain 公平性 | 0.591 | 0.597 | >0.85 |
| 达标帧（Jain≥0.85） | 2.8%（2/71） | 1.4%（1/71） | >50% |
| 平均队头等待 Wt | 0.68s | 0.65s | <0.05s |
| 全网平均控制冲突 Cctrl | **0.57** | 0.80 | ≈0 |

### 问题分析

**1. Jain 公平性严重不足（核心问题）**

Jain 指数均值约 0.59，每帧在 0.19~0.90 之间剧烈波动，大多数帧远低于 0.85 目标。
原因分析：
- **拓扑不对称**：Node 0 仅连 1 个邻居，Node 3 连 4 个，竞争强度天然不等。Node 1/3/6/8 因邻居多导致冲突率（Cctrl 0.73~1.72）显著高于 Node 0/2/4/5/7（Cctrl ≈ 0）。
- **RL 公平性激励不足**：原 `r_gamma=0.3` 公平性权重偏低，策略倾向于最大化吞吐量而忽视公平性。

**2. 队头等待时间严重超标**

所有节点平均 Wt 在 0.6~1.3s（目标 <0.05s）。中心节点（Node 1/3/6）最差，这些节点连接数最多（4个邻居），时隙争抢最激烈。

**3. Tx 成功率未达标**

59.3% < 目标 70%。但相比历史（50.9%），RL 已使成功率提升 8.4pp，控制冲突减少 29%，说明 RL 策略对吞吐量有正向作用。

**4. RL 策略从崩溃后 checkpoint 恢复**

本次加载的 `tdma_ppo_latest.pt` 对应 frame ~7000（崩溃后），avg_r 仅 +43（历史高峰 +82~+96）。

### 改进方案（2026-04-11 实施）

1. **从崩溃前 checkpoint 重训**：使用 `checkpoints/tdma_ppo_frame4000.pt`（崩溃前高峰期）
2. **增强公平性激励**：`r_gamma` 0.3 → 0.5，使策略更积极地追求均匀时隙分配
3. **防止熵坍缩**：`ent_coef` 0.05 → 0.1，保持更长时间的策略多样性
4. **减少过拟合**：`ppo_epochs` 4 → 2，每次更新梯度步数减半
5. **延长仿真**：`sim-time-limit` 20s → 100s（约 370 帧，11 次 PPO 更新），获得足够训练信号

启动命令：
```bash
./scripts/run_joint.sh --num_slots 10 --num_nodes 9 \
    --load_ckpt checkpoints/tdma_ppo_frame4000.pt \
    --ent_coef 0.1 --ppo_epochs 2 --r_gamma 0.5
```

---

## 问题 7：RL 从零学习上限分析（2026-04-11 系统实验）

**发现时间**：2026-04-11 多轮实验后综合结论

### 核心发现

经过大量实验，确认：**当前架构的 RL 无法从随机初始化提升至历史 +82~+96 水平**。

#### 数据对比

| 策略来源 | avg_r | 说明 |
|:--|:--|:--|
| C++ 启发式策略 | +82~+96 | 历史"高峰期"实际为启发式输出（ActionSender fix 前 3072 帧） |
| RL 从零学习（任何 ent_coef 配置） | +44~+52 | 与随机策略基线（+49）无显著差异，1760+ 帧无进展 |
| 随机策略基线 | +49 | frame 32 时即可观测，ppo_epochs=2/4 下保持平稳 |

#### 根本原因：共享策略协同问题

所有 9 个节点**共享同一组参数**，加上节点 ID one-hot 嵌入。从随机初始化出发：
1. 所有节点策略完全相同，倾向于选择**同一批时隙** → 大量冲突
2. Advantage 估计噪声大（动作随机，奖励信号弱）
3. 节点 ID one-hot 在随机权重下无法有效区分节点行为，需要协同梯度才能学出分工
4. 当 entropy 下降时，策略收敛到某个随机方向（不一定是协调方向），avg_r 不升反降

#### 历史 +82~+96 的真相（重要修正）

2026-03-29 记录的 "前 4800 帧 avg_r 维持 +82~+96" 中：
- **前 3072 帧**：C++ 使用启发式策略，RL 训练在启发式数据上进行（off-policy 预热）
- **第 3072 帧**：ActionSender 连接成功，RL 开始控制时隙选择
- **3072-4800 帧**：RL 从启发式"热身"后的良好初始点出发，短暂维持高性能后崩溃

这一现象是 Bug（ActionSender 延迟连接），而非 RL 的真实学习能力。ActionSender fix 后，此路径已不可复现。

### 崩溃阈值规律

| 实验 | 崩溃时 entropy | ent_coef |
|:--|:--|:--|
| 2026-03-15 基线 | 0.30 | 0.01 |
| Phase 1b（ppo_epochs=4, ent_coef=0.05） | 0.34 | 0.05 |
| frame7000+自适应（ent_coef=0.10） | 0.22 | 0.10 |

**结论**：崩溃阈值随 ent_coef 下移（更强熵正则可推迟崩溃），但并非"防止崩溃"，只是减慢了熵下降速度。当前实现无法真正稳定 entropy > 0.30。

---

## 问题 8：行为克隆（BC）预训练实验（2026-04-12）

### 8.1 独立 Agent 架构实验结论（问题 7 延续）

| 运行 | 帧数 | 初始 entropy | 最终 entropy | avg_r 范围 | 结论 |
|:--|:--|:--|:--|:--|:--|
| 独立 Agent Run 1（从零） | 1760 | 0.692 | 0.649 | +44~+52 | 无进展 |
| 独立 Agent Run 2（自适应 ent） | 1760 | 0.649 | 0.472 | +44~+52 | 无进展 |
| 独立 Agent Run 3（ent_coef=0.10） | ~1500 | ~0.472 | ~0.360 | +44~+52 | 无进展 |
| 独立 Agent Run 4（ent_coef=0.10） | 1440 | 0.359 | 0.250 | +44~+52 | 无进展 |

**结论**：独立 Agent + 自适应 ent_coef 在 ~6500 帧后仍无法突破 +49 随机基线。根本原因与共享 Agent 相同：单节点 32 样本/更新，梯度噪声过大，无法学出协调分工。

### 8.2 BC 预训练实现

**设计**：
- Python 连接状态管道（读状态），不发送动作 → C++ 自动使用启发式策略
- BC 训练对：`(feat_seq_{t-1}, Pt1_t)` 其中 Pt1_t = 启发式在帧 t-1 的申请概率
- Loss：`MSE(network_output, Pt1)`，lr=1e-3

**BC 训练曲线**（1088 帧，34 次更新）：

```
frame=32  BC_loss=0.1860   ← 初始（全 0 初始化 vs 启发式平均概率）
frame=320 BC_loss=0.1444   ← 快速下降阶段
frame=640 BC_loss=0.0801   ← 继续下降
frame=960 BC_loss=0.0578   ← 趋于收敛
frame=1088 BC_loss=0.0658  ← 平台期（~0.055~0.07）
```

**BC 后策略状态**：entropy ≈ 0.43（适度确定性），avg_r ≈ +13（低于随机基线 +49）

### 8.3 BC 效果分析：为何 avg_r = +13 低于随机基线

**启发式高性能（+82~+96）的真正来源**：

C++ 启发式的核心机制是 `avoidSlotsNextSchedule`：当某时隙上次发生冲突，下次自动降低该时隙申请概率（一次性退避）。这是一种**在线自适应碰撞回避**机制。

BC 训练目标 `Pt1` 是"特定上下文下启发式决策的平均"，缺少了这个自适应部分：

| 机制 | 启发式策略 | BC-RL 策略 |
|:--|:--|:--|
| 基础时隙偏好 | 有（基于队列长度、邻居状态） | 有（BC 学到平均偏好） |
| 碰撞自适应退避 | **有**（avoidSlotsNextSchedule） | **无**（输出固定概率，不响应上一帧碰撞） |

结果：BC 策略在争用时隙上反复请求，导致**持续性碰撞** → avg_r < 随机基线。

### 8.4 BC + RL 微调实验

| 参数组合 | 初始 avg_r | 最终 avg_r（14 更新后） | entropy 趋势 | 结论 |
|:--|:--|:--|:--|:--|
| ent_coef=0.10（第一次，帧对齐 bug） | +21 | -157 | 0.43→0.53 ↑ | 崩溃 |
| ent_coef=0.01（修复对齐后） | +13 | -176 | 0.43→0.49 ↑ | 崩溃 |

**崩溃模式分析**：
1. BC 策略持续请求争用时隙 → 大量冲突 → 负奖励
2. RL 梯度（A < 0）推动策略远离这些动作 → entropy 上升（更随机）
3. 随着 entropy 上升，策略变成"random RL"（avg_r ≈ +49）→ 但过程中经过 avg_r < 0 的谷底
4. 最终仍然收敛到 +49 随机基线（14 更新不够到达）

**对帧对齐 bug 的修正**：
```python
# Bug（原始）：feat_seq_prev 是添加当前 feat 之前的序列 → 2帧错位
feat_seq_prev = stack(window_before_append)  ← 使用时对应 state_{t-2}

# Fix：先 append，再构建当前帧序列
window.append(feat)  # 先加入
feat_seq_curr = stack(window_after_append)   ← 使用时对应 state_{t-1} ✓
```

### 8.5 根本限制总结

| 现象 | 根本原因 |
|:--|:--|
| RL 无法超越 +49 随机基线 | 样本效率不足（32 样本/节点/更新），梯度太噪，无法学出协调 |
| BC 启动策略低于随机基线 | BC 学到的是启发式"平均动作"，缺少 avoidSlotsNextSchedule 自适应退避 |
| BC + RL 微调崩溃 | BC 初始策略差（+13），RL 梯度需推翻 BC 结构才能改善，过程产生极大负奖励 |

### 8.6 下一步：RL 与启发式协同方案（推荐）

**核心思路**：让 RL 学习**调整**启发式，而不是**替代**启发式。

在 C++ 中修改 `scheduleRequests()`：
```cpp
// 当前：直接用 RL 概率替代启发式
float p = useRlAction ? getRlActionProb(slot) : heuristicPriority(slot);

// 建议：RL 输出乘数，叠加在启发式概率上
float heuristic_p = computeHeuristicPriority(slot);  // 含 avoidSlotsNextSchedule
float rl_factor   = getRlActionFactor(slot);          // RL 输出 [0.5, 2.0]，中性值=1.0
float p = clamp(heuristic_p * rl_factor, 0.0, 1.0);
```

好处：
- 启发式的自适应退避机制**始终生效**
- RL 只负责"调高/调低特定节点对特定时隙的偏好"（细粒度调优）
- 从中性初始化（rl_factor=1.0）出发，RL 有可能改善启发式

此方案需要 C++ 修改，预计工作量 2~3 天。

### 当前推荐启动命令

从零训练独立 Agent（用于对比基准）：
```bash
./scripts/run_joint.sh --num_slots 10 --num_nodes 9 \
    --ppo_epochs 2 --r_gamma 0.5 --lr_decay_gamma 0.9995
```

BC 预训练（1500 帧）：
```bash
# [已废弃] BC 在乘数模式下被禁用（--bc_frames 会抛 ValueError）
# ./scripts/run_joint.sh --num_slots 10 --num_nodes 9 --bc_frames 1500 --bc_lr 1e-3
```

BC + RL 微调（结果不佳，根本原因见问题 9）：
```bash
# [已废弃] BC 策略 avg_r ≈ +13，乘数模式已替代 BC 路线
```

---

## 问题 9 — RL 乘数模式（Multiplier Mode）

**发现时间**：2026-04-12

**问题描述**：

历次 RL 实验（直接替换概率、独立 Agent、BC 预训练）均无法突破随机基线 +49，而启发式稳定 +82~+96。根本原因诊断：

- **RL 接口直接替换申请概率**：`reqProb = rlProb`（C++ `scheduleRequests()`）
- **`avoidSlotsNextSchedule` 退避机制通过 `slotOrder` 生效**，但 RL 可以对退避时隙赋予高概率，完全绕过退避效果
- **BC 预训练**学到"平均启发式"，输出 0.6~0.9 概率，同样绕过退避 → avg_r ≈ +13（低于随机基线 +49）

**解决方案：RL 乘数模式**

**已实施（2026-04-12）**，改动文件：

| 文件 | 改动 |
|:---|:---|
| `DynamicTDMA.cc` `scheduleRequests()` | 始终计算 `heurProb`，RL 输出 α 作为乘数因子 |
| `DynamicTDMA.h` | 新增 `myHeurProbs` 成员变量 + `RlFrameFeatures.heurProbs` 字段 |
| `rl/rl_agent.py` | `actor_head` 零初始化 + 特征追加 HeurProb（M维）+ `input_dim: 4M→5M` |
| `rl/rl_receiver.py` | `NodeObservation` 新增 `HeurProb` 字段 |
| `rl/ppo_trainer.py` | BC 预训练废弃（`--bc_frames` 抛 ValueError） |

**核心逻辑（C++ `scheduleRequests()`）**：

```cpp
// 旧版（退避失效）
reqProb = rlProb;  // 直接替换，绕过启发式

// 新版（乘数模式，退避保留）
double heurProb = max(dynamicPrio, minProb) + highLoadBoost;
myHeurProbs[slot] = heurProb;
if (getRlActionProb(slot, rlAlpha)) {
    reqProb = clamp(heurProb * rlAlpha * 2.0, 0.0, 1.0);
    // rlAlpha≈0.5 → 乘数=1.0，等效纯启发式
    // rlAlpha→1.0 → 乘数=2.0，放大（最大为2×heurProb）
    // rlAlpha→0.0 → 乘数≈0，抑制该时隙
}
```

**乘数上限分析（max = min(1.0, 2×heurProb)）**：

| 优先级 | heurProb 最小值 | RL 最大 reqProb | 说明 |
|:---|:---|:---|:---|
| 高优先级 | 0.6 | min(1.0, 1.2) = 1.0 | RL 可强制全满 |
| 低优先级 | 0.2 | min(1.0, 0.4) = 0.4 | 保留优先级排序 |

结论：满足优化需求。若后续需更强低优先级扩展能力，可将系数 2.0 改为 3.0。

**网络初始化改动**：

```python
# rl_agent.py LSTMActorCritic.__init__()
self.actor_head = nn.Linear(lstm2_hidden, num_slots)
nn.init.zeros_(self.actor_head.weight)   # 新增
nn.init.zeros_(self.actor_head.bias)     # 新增
# Sigmoid(0) = 0.5 → 初始乘数 = 1.0，训练起点等价纯启发式
```

**状态向量变化**：

```
旧版: Bown(M) + T2hop(2M) + scalar(10) + Pt1(M) + NodeID(N) = 4M+10+N = 59维
新版: Bown(M) + T2hop(2M) + scalar(10) + Pt1(M) + HeurProb(M) + NodeID(N) = 5M+10+N = 69维
```

新增 `HeurProb`（本帧启发式概率向量）让 RL 感知乘数基准，便于学习精确调整策略。

**注意**：新状态维度与旧 checkpoint（59维）不兼容，需从零开始训练。

**实验结果（2026-04-12 验证）**：

| 指标 | 纯启发式基线 | RL 直接替换 | BC 预训练 | RL 乘数模式（实测） |
|:---|:---|:---|:---|:---|
| avg_r 初始值 | +44~+49 | +44~+52 | +13 | +44（α=0.5，等效启发式） |
| avg_r 稳定值 | +44~+49 | +44~+52 | +13 | +44（1760帧无提升） |

**基线修正**：文档曾声称纯启发式基线 avg_r ≈ +82~+96，经验证为旧代码（`494c4e4` 项目重组前）的产物，当前代码下纯启发式基线为 **+44~+49**。历史 +82~+96 因 DynamicTDMA.cc 的 Bug 修复和同步机制变更已不可复��。所有保存的日志（2026-04-11~12）均证实从零开始的 avg_r 在 +42~+49 范围。

**推荐训练命令**：

```bash
# 从零训练（乘数模式，无需 BC）
./scripts/run_joint.sh --num_slots 10 --num_nodes 9
# 注意：不加 --load_ckpt，旧 checkpoint（59维）与新网络（69维输入）不兼容
```

---

## 改进 5：网络瘦身 + 训练节奏 + 奖励重塑（三合一）

**实施时间**：2026-04-12

**问题诊断**：乘数模式实施后，RL 训练 1760 帧仍无法超越启发式基线（avg_r ≈ +44）。根因分析：

1. **网络严重过参数化**：旧版 LSTM-1(128) + LSTM-2a(64) + LSTM-2c(64) 共 ~192K 参数，但每次 PPO 更新仅 32 帧样本（9节点 × 32帧 = 288 条数据），参数/样本比 > 600:1，梯度被噪声完全淹没
2. **更新间隔过短**：`update_every=32` 导致每次 PPO 更新的轨迹过短，GAE 估计方差极大
3. **奖励信号弱**：原始 avg_r ≈ +44 波动范围 ±10，PPO 无法区分好坏动作

### 改动 A：网络瘦身（`rl/rl_agent.py`）

去掉 LSTM-2 层，LSTM-1 hidden 从 128 缩至 32：

```
旧版（~192K 参数）:
  输入(69) → LSTM-1(128) → Actor: LSTM-2a(64) → Linear(10) → Sigmoid
                          → Critic: LSTM-2c(64) → Linear(1)

新版（~13.5K 参数）:
  输入(69) → LSTM-1(32) → Actor: Linear(10) → Sigmoid（零初始化）
                         → Critic: Linear(1)
```

参数量对比：
- 旧版：LSTM-1(4×128×(69+128+1))=101K + LSTM-2a(4×64×(128+64+1))=49K + LSTM-2c(49K) + 头部 ≈ 192K
- 新版：LSTM-1(4×32×(69+32+1))=13K + actor_head(32×10)=320 + critic_head(32×1)=32 ≈ 13.5K

**关键保留**：actor_head 零初始化（Sigmoid(0)=0.5 → 乘数=1.0 → 等效纯启发式起点）。

### 改动 B：增大更新间隔（`rl/ppo_trainer.py`）

`update_every` 从 32 → 128。每次 PPO 更新的样本量从 288 增至 1152（9节点 × 128帧），梯度估计更稳定。

### 改动 C：差分奖励重塑（`rl/ppo_trainer.py`）

每节点维护 EWMA 奖励基线（衰减系数 0.95），存入 buffer 的奖励为差分值：

```python
raw_r = compute_reward(obs)
baseline = reward_baselines[nid]
reward_baselines[nid] = baseline + 0.05 * (raw_r - baseline)
shaped_r = raw_r - baseline  # 存入 buffer
```

效果：消除奖励绝对值的干扰，策略梯度只关注"比历史平均好还是差"，减小方差。日志 `avg_r` 仍用原始奖励便于历史对比。

### 首次实验结果（2026-04-12）

```
[PPO] update_every=128  ppo_epochs=4  lr=0.0003
[PPO] frame=  128  update=   1  avg_r=+181.637  L_critic=1.0250  entropy=0.6931
[PPO] frame=  512  update=   4  avg_r=+183.584  L_critic=1.0167  entropy=0.6931
[PPO] frame= 1024  update=   8  avg_r=+175.641  L_critic=0.9654  entropy=0.6931
[PPO] frame= 1664  update=  13  avg_r=+179.809  L_critic=0.9405  entropy=0.6930
```

注意 avg_r ≈ +180 是 128 帧累积值（等效每帧 +1.4，与旧版 +44/32帧 = +1.38/帧一致）。

**观察**：
- L_critic 从 1.025 平稳下降至 0.94，Critic 正在学习价值估计
- entropy ≈ 0.6931（= ln2，Bernoulli(0.5) 最大值），策略尚未开始偏移
- 13 次 PPO 更新太少，需更长仿真才能观察策略学习信号

**兼容性**：新网络与旧 checkpoint 不兼容（结构不同），需从零训练。

### 改进 5.1：二值动作 + 奖励延迟对齐

**实施时间**：2026-04-12（紧接改进 5）

**发现的两个架构级 Bug**：

#### Bug 1：动作-环境断开

原代码将网络输出的**连续 α ≈ 0.5 发送给 C++**，而 PPO 优化的是**二值采样动作**。C++ 始终收到 α≈0.5 → 乘数=1.0 → reqProb=heurProb，环境行为恒定不变。PPO 的 advantage 对 a=0 和 a=1 相同，梯度正负抵消 → L_actor ≈ 0。

**修复**：改为发送 Bernoulli 采样的二值动作 `node_actions`（0或1），C++ 乘数模式下 action=1 → 乘数=2.0（强申请），action=0 → 乘数=0.0（不申请）。

#### Bug 2：奖励时序错位

帧 t 的 Nsucc/Ncoll 反映帧 t-1 的调度结果（因 C++ `writeRlFeatures()` 在数据阶段末尾调用，Nsucc 是本帧已完成传输的统计量，由帧 t 的 `scheduleRequests()` 决定，而该调度使用的 RL 动作来自帧 t-1 的 `readRlActions()`）。

原代码将帧 t 的奖励 r_t 和帧 t 的动作 a_t 配对，但 r_t 实际是 a_{t-1} 的结果 → advantage 不反映 a_t 的质量 → 梯度是噪声。

**修复**：引入 `pending_transitions` 字典，帧 t 的 transition 在帧 t+1 到来时才用 r_{t+1} 完成，确保 (s_t, a_t, r_{t+1}) 因果对齐。

#### 实验对比（83 次 PPO 更新）

| 实验 | 修复内容 | entropy 变化 | L_actor |
|:---|:---|:---|:---|
| 发连续 α | 无 | 0.6931→0.6923（Δ=-0.0008） | ≈ 0 |
| 二值动作 | Bug 1 | 0.6931→0.6927*（28次更新） | ≈ 0 |
| 二值+延迟对齐 | Bug 1+2 | 0.6931→0.6915（**Δ=-0.0016**） | ≈ 0 但有微弱趋势 |

修复后 entropy 下降速度翻倍，但仍然极慢。L_critic 持续下降（1.0→0.55），Critic 学习正常。

#### 残余问题：多智能体信用分配

9 节点独立学习，每节点的 Nsucc/Ncoll 同时受自身动作和其他 8 个节点的随机动作影响。单节点动作的边际效应被多智能体噪声淹没，策略梯度信噪比极低。这是独立 Agent + 局部观测约束下的固有困难。

**可能的后续方向**：
1. **增大 update_every 至 512+**：更多样本平均掉多智能体噪声
2. **差分奖励 baseline 使用 Critic 的 V(s)**：用 GAE 已有的 advantage 估计代替简单 EWMA，更好地减小方差
3. **渐进式探索**：初期保持 α≈0.5（少偏离启发式），缩小探索范围降低其他节点噪声的影响
4. **增大学习率**：小梯度需要大步长来穿过噪声（但有发散风险）

---

## 改进 6：逐时隙奖励分解（Per-Slot Reward Decomposition）

**日期**：2026-04-13

### 问题诊断

改进 5.1 后 entropy 下降速度仍极慢（0.0016/83 次更新），根因分析发现**聚合奖励函数是核心瓶颈**：

1. **信用分配缺失**：每节点每帧做 M=10 个独立二值动作，但只收到 1 个标量奖励（Nsucc - Ncoll + Jlocal - Wt）。PPO 无法区分哪个时隙的决定好、哪个坏。
2. **Jlocal/Wt 增加噪声**：Jlocal 是全网聚合指标，Wt 受流量随机性影响大，对单帧时隙选择几乎无有效梯度信号。
3. **多智能体噪声被聚合放大**：单个时隙动作的边际贡献 ≈±1，但奖励总方差来自 10 个时隙叠加 × 9 节点噪声。

**信噪比估算**：单时隙动作对聚合奖励的信噪比 ≈ 1/(√10 × 多节点因子) ≈ 1/30，PPO 梯度被噪声淹没。

### 改进方案

**方向 A：逐时隙奖励分解**

#### C++ 端改动

`writeRlFeatures()` 新增 `SlotResult` 字段（M 位字符串），逐时隙标记：
- `'0'` = 未申请（myPriorities[s] ≤ 0）
- `'1'` = 申请且成功（mySlots[s] == true）
- `'2'` = 申请但失败（mySlots[s] == false）

```json
"reward_signal": {"Nsucc":3, "Ncoll":1, "SlotResult":"0012001010", ...}
```

#### Python 端改动

1. **逐时隙奖励**（`compute_per_slot_reward`）：
   ```python
   r_s = +1.0  if SlotResult[s] == '1'  # 成功
   r_s = -1.0  if SlotResult[s] == '2'  # 失败
   r_s =  0.0  if SlotResult[s] == '0'  # 未申请
   ```

2. **逐时隙 Critic**：`critic_head` 从 `Linear(32, 1)` → `Linear(32, M)`，每个时隙独立价值估计 V_s(t)。

3. **逐时隙 PPO**：
   - ratio 不再对 M 维 log_prob 求和，改为逐时隙 `ratio_s = exp(log_prob_s - log_prob_old_s)`
   - advantage 为 (N, M) 形状，每时隙独立 GAE
   - Clipped surrogate 逐时隙计算后取均值

4. **逐时隙 EWMA 基线**：`reward_baselines[nid]` 从标量改为 (M,) 向量，差分奖励逐时隙独立。

#### 涉及文件

| 文件 | 改动 |
|:---|:---|
| `DynamicTDMA.h` | `RlFrameFeatures` 新增 `slotResult` 字段 |
| `DynamicTDMA.cc` | 构造 `slotResult` 字符串 + JSON 输出 |
| `rl/rl_receiver.py` | `NodeObservation` 新增 `SlotResult` 属性 |
| `rl/rl_agent.py` | 新增 `compute_per_slot_reward()`；Critic 输出 M 维 |
| `rl/ppo_trainer.py` | `NodeTransition` 改为逐时隙；GAE/PPO 逐时隙更新 |

### 实验结果（83 次 PPO 更新对比）

| 指标 | 聚合奖励（改进 5.1） | **逐时隙奖励（改进 6）** | 倍数 |
|:---|:---|:---|:---|
| Entropy 变化 | 0.6931→0.6915（Δ=0.0016） | **0.6931→0.5249（Δ=0.168）** | **105x** |
| 每次更新 Entropy 下降 | 0.000019 | **0.0020** | **105x** |
| L_actor | ≈ 0（无学习信号） | **+0.0076（活跃学习）** | 0→有 |
| L_critic | 正常下降 | 稳定 ~0.98 | — |
| 自适应 ent_coef 触发 | 未触发 | **entropy=0.52 时触发** | — |

```
[PPO] frame=  128  update=   1  avg_r=-302.556  entropy=0.6931  ent=0.050
[PPO] frame= 2560  update=  20  avg_r=-305.111  entropy=0.6911
[PPO] frame= 5120  update=  40  avg_r=-271.444  entropy=0.6787
[PPO] frame= 7680  update=  60  avg_r=-285.556  entropy=0.6114
[PPO] frame= 8960  update=  70  avg_r=-284.889  entropy=0.5532
[PPO] frame=10240  update=  80  avg_r=-280.333  entropy=0.5169  ent=0.068
[PPO] frame=10624  update=  83  avg_r=-244.889  entropy=0.5249  ent=0.068
```

### 分析

1. **逐时隙信用分配有效**：每个 Bernoulli 策略独立接收该时隙的成功/失败反馈，梯度信噪比从 ~1/30 提升至 ~1/1，entropy 收敛提速 100x+。

2. **avg_r 为负是正常现象**：逐时隙奖励的 avg_r = Σ(+1 成功, -1 失败) 累积 128 帧 × 9 节点平均。二值动作模式下，action=0 → 乘数=0（完全屏蔽该时隙），action=1 → 乘数=2（加倍申请）。随机策略（entropy≈0.693）约一半时隙被屏蔽、一半加倍申请，比纯启发式差。但 avg_r 从 -302 上升到 -244，趋势正确。

3. **avg_r 量级与旧版不可直接比较**：旧版 avg_r = Nsucc - Ncoll + 0.3*Jlocal - 0.2*Wt ≈ +44~+49，新版 avg_r = Σ(逐时隙 ±1) × 128 帧 / 9 节点，量级和含义不同。

### 兼容性说明

- Critic 从 1 输出改为 M 输出，旧 checkpoint 不兼容，需从零训练
- C++ 端 JSON 新增 `SlotResult` 字段，Python 端做了兼容处理（缺失时默认空字符串）

### 失败实验：柔化二值乘数（方向 1）

**改动**：将 C++ 乘数映射从 `heurProb * 2α`（α∈{0,1} → 乘数∈{0,2}）改为 `heurProb * (0.5+α)`（乘数∈{0.5,1.5}），期望随机策略平均乘数=1.0、起点接近启发式基线。

**结果**：entropy 收敛反而慢了 6 倍（83 次更新仅降 0.028 vs 硬乘数降 0.168），L_actor 弱了 38 倍。

**原因**：柔乘数下 action=0 → 节点仍有 ~30% 概率申请，action=1 → 概率仅增 50%。两种动作对环境的影响差异太小，逐时隙奖励的信号被稀释。硬乘数（0 vs 2）让 action=0 确定性地不申请（reward=0），action=1 高概率申请（reward=±1），信号干净。

**结论**：在二值动作框架下，信号强度比起点位置更重要。已回滚。

### 失败实验：连续动作 Squashed Gaussian（方向 2）

**改动**：将 Bernoulli 离散动作替换为 Squashed Gaussian 连续动作——Actor 输出原始 logits μ，新增可学习参数 `actor_log_std`（初始 -0.7 → std≈0.5），采样 z~N(μ,σ²) 后 action=sigmoid(z)∈(0,1)，C++ 乘数=2α∈(0,2)。log_prob 使用 Jacobian 校正：`Normal(μ,σ).log_prob(z) - log(a(1-a))`。

**预期**：连续动作提供平滑梯度，初始 95% 乘数范围 (0.54, 1.46)，避免 binary {0,2} 的破坏性探索。

**实际结果（83 次更新对比）**：

| 指标 | Binary Bernoulli | Squashed Gaussian |
|:---|:---|:---|
| entropy Δ | **-0.168**（收敛） | +0.037（发散） |
| L_actor | **0.0076**（活跃） | ≈0.0000（死寂） |
| avg_r | ≈-300 | ≈-300 |

**原因分析**：
1. **entropy 发散**：`actor_log_std` 可学习，entropy bonus 对 log_std 的梯度恒为 +ent_coef（常数推力），PPO 信号太弱（L_actor≈0）无法抵消，std 持续增大
2. **L_actor 死寂**：环境结果本质是离散的（成功/失败），Bernoulli 的 log_prob 梯度天然与离散结果对齐（action=1 申请成功 → 增大概率；失败 → 减小），而 Gaussian 的梯度经过 sigmoid 压缩和 Jacobian 校正后过于弥散
3. **信号不匹配**：per-slot reward ∈ {+1, -1, 0} 是离散信号，与 Bernoulli 离散动作配合最佳；连续动作在此环境无优势

**结论**：对于结果离散（成功/失败）的时隙调度问题，Bernoulli 是正确的动作分布，不应换为连续分布。应在 Bernoulli 框架内解决探索问题。已回滚。

---

## 改进 7：训练超参数与时长调优（三方向对比实验）

**发现时间**：2026-04-18

**问题**：逐时隙奖励（改进 6）后，83 次 PPO 更新（3000s 仿真）内 entropy 收敛但 avg_r 未改善（仍 ≈-274）。需确定是训练不足、奖励信号弱还是超参不当。

**实验设计**：三个方向独立对比，仅改变一个变量。

### 方向 1：延长训练（15000s → 418 次更新）

**改动**：`sim-time-limit` 从 3000s 增至 15000s，其余参数不变（ent_coef=0.05）。

**结果**：

| 阶段 | update | avg_r | entropy | L_actor | L_critic |
|:---|:---|:---|:---|:---|:---|
| 起点 | 1 | -274 | 0.693 | -0.0006 | 0.986 |
| 拐点前 | 83 | -249 | 0.512 | +0.0128 | 0.992 |
| 快速改善 | 100 | -191 | 0.471 | +0.0189 | 0.948 |
| 持续上升 | 150 | -90 | 0.275 | +0.0083 | 0.728 |
| 接近基线 | 200 | -44 | 0.190 | +0.0064 | 0.460 |
| 超越基线 | 300 | -34 | 0.110 | +0.0046 | 0.353 |
| 收敛 | 418 | **-16** | 0.076 | +0.0032 | 0.149 |

**总改善**：avg_r 从 -274 → -16，**Δ = +258**。L_critic 从 0.99 → 0.15（价值函数校准良好）。

**关键发现**：3000s 仿真（83 次更新）恰好停在 avg_r 改善的拐点——entropy 已收敛但策略尚未将学到的结构转化为性能提升。延长到 418 次更新后，策略从"学会收敛"进化到"学会调度"。

### 方向 2：未申请惩罚（reward shaping）

**改动**：`compute_per_slot_reward` 中，当 Qt>0 时对 SlotResult='0'（未申请）给 -0.2 惩罚。期望激励更积极申请。

**结果（83 次更新）**：

| 指标 | 基线（无惩罚） | 方向 2（-0.2 惩罚） |
|:---|:---|:---|
| entropy | 0.693 → 0.512 (Δ=-0.181) | 0.693 → **0.496** (Δ=-0.197) |
| L_actor | +0.0128 | +0.0025 |
| avg_r | -274 → -249 | -493 → -452 |
| Δ_avg_r | +25 | +41（归一化后 ≈ -252） |

**分析**：entropy 收敛最快（额外惩罚增加了非零奖励占比），但 L_actor 弱（0.0025 vs 基线 0.0128），说明惩罚项引入了噪声，削弱了 PPO 信号。归一化 avg_r 与基线相当，无显著优势。

### 方向 3：降低 ent_coef（0.01/0.03）

**改动**：`ent_coef` 从 0.05 → 0.01，`ent_coef_high` 从 0.10 → 0.03。

**结果（83 次更新）**：

| 指标 | 基线（ent_coef=0.05） | 方向 3（ent_coef=0.01） |
|:---|:---|:---|
| entropy | 0.693 → 0.512 (Δ=-0.181) | 0.693 → **0.527** (Δ=-0.166) |
| L_actor | +0.0128 | **+0.0115** |
| avg_r | -274 → -249 | -274 → **-244** |

**分析**：entropy 收敛稍慢但 L_actor 同样活跃（0.0115 vs 0.0128），avg_r 稍优（-244 vs -249）。低 ent_coef 减少了对策略收敛的阻力，但 83 次更新内差异不大——延长训练后效果趋同。

### 综合结论

1. **主瓶颈是训练时长**：3000s（83 次更新）远远不够。策略需要 ~100 次更新完成 entropy 收敛，之后 avg_r 才开始改善。推荐 sim-time-limit ≥ 15000s（400+ 次更新）。
2. **奖励重塑效果有限**：基础的 {+1, -1, 0} per-slot 奖励信号已足够强，额外惩罚引入噪声反而削弱学习。
3. **ent_coef 影响边际**：低 ent_coef 加速早期收敛但长期效果趋同。默认 0.05 + 自适应机制工作良好。
4. **学习曲线三阶段**：(a) entropy 收敛期（0~80 更新），(b) 策略改善期（80~200 更新，avg_r 快速上升），(c) 精细调优期（200+ 更新，缓慢收敛到 -16）。

## 改进 8：纯启发式基线实测（2026-04-19）

### 背景

avg_r 指标一直呈现负值（RL 训练末期 ≈ -0.9），需验证"负值"是否代表策略失败。通过直接测量纯启发式（无 RL）下的 avg_r，建立真实基线。

### 测量方法

修改 Python 端为**只监听不回传**模式：读取 C++ 状态管道并用 `compute_per_slot_reward()` 计算 avg_r，但不发送动作。C++ 因收不到 RL 动作回退到纯启发式策略。

- 仿真时长：3000s（≈10714 帧，83 个窗口）
- 拓扑：9 节点 × 10 时隙（与 RL 实验一致）

### 结果对比

| 策略 | avg_r 均值 | avg_r 范围 | 每帧每节点净奖励 |
|:---|:---|:---|:---|
| **纯启发式（无 RL）** | **-306.035** | [-362.222, -247.222] | ≈ -2.4 |
| **RL 训练末期（30000s, 837 次更新）** | ≈ -0.9 | [-10.3, 0.0] | ≈ -0.007 |

**提升倍数**：约 **340 倍**（avg_r 从 -306 → -1）。

### 结论

1. **RL 策略极其有效**：将每帧每节点的平均净奖励从 -2.4 提升到接近 0。在 10 时隙/9 节点的密集竞争场景下，RL 几乎完全消除了碰撞。
2. **"负值"的真正含义**：avg_r ≈ -1 不是"策略失败"，而是**接近理论最优**——要么成功传输（+1），要么选择不申请（0），碰撞极少。
3. **启发式的问题**：纯启发式会为所有有数据的时隙申请发送，导致大量碰撞（9 节点竞争 10 时隙，每帧约 2.4 次碰撞/节点）。RL 学到了"智能让渡"策略，主动避开高风险时隙。
4. **对后续工作的启示**：avg_r 已接近零，进一步提升空间有限。若追求更高吞吐，应将奖励从 {+1, -1, 0} 改为延迟敏感或吞吐量相关指标。

---

## 改进 9：邻居密度自适应 clamp + KL-to-Heuristic 正则（消融实验，2026-04-19）

### 背景

文献调研（[分析其他论文-修正版.md](分析其他论文-修正版.md)）提出 6 条改进方向，结合 DynamicTDMA 的已踩坑（改进 4 柔乘数/改进 2 Squashed Gaussian 均失败）优先做两条**低成本、可消融验证**的：

- **方向 D**（Zhou et al., TWC 2023 IoV Caching）：当前 `reqProb = clamp(heurProb × 2α, 0, 1)` 对所有节点使用固定系数 2.0，不考虑拓扑密度。Node 1/3/6 有 3 个邻居应压缩探索空间，Node 0/2/4/5/7 仅 1 个邻居可放大。改为 `mult_max = 2.0 / (1.0 + 0.2 × numOneHopNeighbors)`。
- **方向 B**（Kim et al., DS-PPO TWC 2025）：当前 PPO loss 无任何代价让 α 远离 0.5（启发式等效点）。训练后期 entropy=0.076 意味着 α 已极端化，可能**在本可用启发式的稳定时隙盲目偏离**。加 `λ × (α - 0.5)²` 软正则约束 RL 仅在需要时偏离启发式。

两者都是 **flag-gated 消融**：默认关闭，不影响现有 baseline；通过 CLI/ini 开关启用，方便 A/B 对比。

### 预期邻居密度乘数映射（方向 D）

| 节点 | 一跳邻居数 | multMax (D=on) |
|:---|:---|:---|
| 0, 2, 4, 5, 7 | 1 | 2.0/1.2 ≈ 1.67 |
| 8 | 2 | 2.0/1.4 ≈ 1.43 |
| 1, 3, 6 | 3 | 2.0/1.6 = 1.25 |

### 代码改动

| 文件 | 改动 |
|:---|:---|
| [DynamicTDMA.h](../DynamicTDMA.h) | `adaptiveMultEnabled` + `numOneHopNeighbors` 成员 |
| [DynamicTDMA.ned](../DynamicTDMA.ned) | `adaptiveMultiplier` 参数声明（默认 false）；`metricsOutputDir` 支持按 run 隔离网络指标 |
| [DynamicTDMA.cc](../DynamicTDMA.cc) | `initialize()` 缓存邻居数；`scheduleRequests()` 按开关选乘数；优先将 `slot_stats.csv` / `fairness.csv` / `frame_metrics.csv` 写入 `metricsOutputDir` |
| [omnetpp.ini](../omnetpp.ini) | `**.nodes[*].adaptiveMultiplier = false` |
| [rl/ppo_trainer.py](../rl/ppo_trainer.py) | `PPOConfig.heur_deviation_coef`、PPO loss 追加 `λ(α-0.5)²`、`--seed` 参数（消融公平性） |
| [scripts/run_joint.sh](../scripts/run_joint.sh) | 透传 `--heur_deviation_coef` / `--seed` / `--save_dir` / `--metrics_dir` |
| [scripts/ablation.sh](../scripts/ablation.sh) | 新增 4 组 × 3 seed 消融驱动，自动修改 omnetpp.ini + 恢复 + save_dir / metrics 隔离 |
| [scripts/summarize_ablation_metrics.py](../scripts/summarize_ablation_metrics.py) | 汇总 PPO 末尾指标和网络层 CSV，生成 `metrics_summary.csv` / `metrics_group_summary.csv` |

### 公平性保障

1. **相同种子下网络初始权重一致**：ppo_trainer 新增 `--seed`，同时设置 `torch.manual_seed()` 和 `np.random.seed()`
2. **每次运行独立 save_dir**：ablation.sh 将每次 run 的 ckpt 写到 `logs/ablation_*/GROUP/seedS/checkpoints/`，互不覆盖
3. **每次运行从零训练**：不加载任何已有 checkpoint，4 组起点完全一致
4. **omnetpp.ini 每次备份恢复**：trap EXIT 保证脚本中断也能恢复

### 实验设计

| 实验 | `adaptiveMultiplier` | `--heur_deviation_coef` |
|:---|:---|:---|
| **baseline** | false | 0.0 |
| **D only** | true  | 0.0 |
| **B only** | false | 0.01 |
| **D + B**  | true  | 0.01 |

每组 3 个随机种子（1/2/3），仿真 15000s（≈ 53500 帧 / 418 次 PPO 更新）。

### 完整消融结果（3 seed，15000s）

有效结果来自 3 个日志根目录：

- `logs/ablation_20260419_220819`：seed1 全部组、baseline/seed2
- `logs/ablation_20260425_193503`：D/B/DB 的 seed2 重跑
- `logs/ablation_20260425_195520`：seed3 全部组

说明：`logs/ablation_20260419_220819/D/seed2` 在 update=218 中断，未纳入统计；D/seed2 使用 2026-04-25 重跑的完整 15000s 结果。

| 组 | seed | updates | avg_r | entropy | L_critic | heur_dev | 日志 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| baseline | 1 | 418 | **-296.667** | 0.5738 | 0.9600 | - | `logs/ablation_20260419_220819/baseline/seed1` |
| baseline | 2 | 418 | **-16.667** | 0.0881 | 0.1597 | - | `logs/ablation_20260419_220819/baseline/seed2` |
| baseline | 3 | 418 | **-13.000** | 0.0735 | 0.1381 | - | `logs/ablation_20260425_195520/baseline/seed3` |
| D | 1 | 418 | **-14.667** | 0.0769 | 0.1903 | - | `logs/ablation_20260419_220819/D/seed1` |
| D | 2 | 418 | **-11.000** | 0.0618 | 0.1269 | - | `logs/ablation_20260425_193503/D/seed2` |
| D | 3 | 418 | **-14.667** | 0.0724 | 0.1457 | - | `logs/ablation_20260425_195520/D/seed3` |
| B | 1 | 418 | **-12.667** | 0.0703 | 0.1701 | 0.2368 | `logs/ablation_20260419_220819/B/seed1` |
| B | 2 | 418 | **-12.000** | 0.0619 | 0.1356 | 0.2388 | `logs/ablation_20260425_193503/B/seed2` |
| B | 3 | 418 | **-13.333** | 0.0750 | 0.1730 | 0.2358 | `logs/ablation_20260425_195520/B/seed3` |
| DB | 1 | 418 | **-25.333** | 0.0874 | 0.2824 | 0.2327 | `logs/ablation_20260419_220819/DB/seed1` |
| DB | 2 | 418 | **-12.667** | 0.0683 | 0.1328 | 0.2373 | `logs/ablation_20260425_193503/DB/seed2` |
| DB | 3 | 418 | **-13.111** | 0.0747 | 0.1367 | 0.2358 | `logs/ablation_20260425_195520/DB/seed3` |

### 汇总统计

| 组 | avg_r 均值 | avg_r 样本标准差 | entropy 均值 | L_critic 均值 | heur_dev 均值 | 结论 |
|:---|:---|:---|:---|:---|:---|:---|
| baseline | **-108.778** | 162.727 | 0.2451 | 0.4193 | - | seed1 崩溃导致均值和方差极差 |
| D | **-13.445** | 2.117 | 0.0704 | 0.1543 | - | 相比 baseline 明显降低方差，收敛稳定 |
| B | **-12.667** | 0.667 | 0.0691 | 0.1596 | 0.2371 | 本轮最稳定，软正则未阻塞学习 |
| DB | **-17.037** | 7.188 | 0.0768 | 0.1840 | 0.2353 | seed1 较差，组合不优于单独 D/B |

**完整观察**：

- **baseline 方差过大**：seed2/3 能收敛到 -17/-13，但 seed1 停在 -297，说明当前从零 PPO 对初始化和随机轨迹敏感。
- **D 提高鲁棒性**：3 个 seed 全部收敛到 -11~-15，说明邻居密度自适应 clamp 能避免 baseline/seed1 的坏路径。
- **B 是当前最稳组合**：3 个 seed 分布在 -12~-13.3，样本标准差仅 0.667；`heur_dev≈0.237` 表示策略仍允许偏离启发式，但偏离幅度受控。
- **DB 没有叠加收益**：DB/seed1 为 -25.333，拉低均值；在当前 λ=0.01 和 D 映射下，两个约束同时启用可能偏保守，暂不作为默认方案。
- **推荐默认候选**：若只看本轮 3 seed 的稳定性，优先保留 B 作为主线；D 可作为降低坏 seed 风险的备选。DB 需要调小正则或重新扫 λ 后再判断。

### 风险与兼容性

- **D**：纯 C++ 参数读取，静态拓扑下零运行时开销。关闭时行为与原版完全一致
- **B**：`heur_deviation_coef=0` 时代码不进入新 if 分支，loss 等价原式。checkpoint 格式不变，与现有 ckpt 100% 兼容
- **不改动**：网络 forward 签名、状态向量维度、动作管道格式、checkpoint 格式——不破坏既有 checkpoint 加载

### 网络指标复跑结果（3 seed，15000s）

为避免继续依赖全局 `results/` 时间戳猜测，2026-04-25 重新跑了一轮完整消融，并将每次 run 的网络指标写入各自目录：

- 结果根目录：`logs/ablation_20260425_204257`
- 每个 run：`GROUP/seedS/metrics/{slot_stats.csv,fairness.csv,frame_metrics.csv}`
- 汇总产物：`metrics_summary.csv`、`metrics_group_summary.csv`
- 完整性：12/12 个 run 均达到 `frame=53504, update=418`，`sim.log` 均到达 `t=15000s`；每个最终帧均有 9 个节点行

| 组 | 完整 run | avg_r | packets/frame | tx_success | Jain 尾 100 | queue 尾 100 | slot_util |
|:---|---:|---:|---:|---:|---:|---:|---:|
| baseline | 3/3 | -19.963 ± 7.370 | 5.655 ± 0.197 | 0.7140 | 0.3741 | 371577.6 | 0.0628 |
| D | 3/3 | -14.926 ± 2.667 | 5.479 ± 0.162 | 0.7137 | 0.3973 | 380503.9 | 0.0609 |
| B | 3/3 | -18.037 ± 3.373 | 5.764 ± 0.232 | 0.7133 | 0.3879 | 365158.5 | 0.0640 |
| DB | 3/3 | -17.148 ± 5.009 | 5.728 ± 0.312 | 0.7156 | 0.3823 | 367273.2 | 0.0636 |

**网络层解释**：

- **D 的 reward 最好，但吞吐最低、队列最高**：`avg_r=-14.926` 优于其他组，但 `packets/frame=5.479` 最低、`queue_tail100=380503.9` 最高。因此 D 更像“避撞保守”策略，不能仅凭 reward 判为网络性能最优。
- **B 的网络层综合表现最好**：`packets/frame=5.764` 最高，`queue_tail100=365158.5` 最低，`slot_util=0.0640` 最高；虽然 reward 不如 D，但更符合吞吐和队列压力目标。
- **DB 次优但不稳定优于 B**：DB 的 `tx_success=0.7156` 最高，吞吐接近 B，但 reward 方差更大且队列略高，不宜直接替代 B。
- **公平性整体仍偏低**：四组 `Jain_tail100` 仅 0.374~0.397，说明 10 时隙/9 节点密集竞争下，当前策略仍存在明显节点间服务不均。

**修正结论**：旧 reward-only 表可作为训练稳定性参考；网络层论文结论应以本次 `metrics/` 复跑为准。若目标是实际 MAC 性能，当前优先推荐 **B only**；D 可作为降低 reward 崩溃风险的备选，但需要结合吞吐和队列惩罚重新调权。

---

## 运行记录 10：低熵 checkpoint 续训完整仿真（2026-04-25）

### 运行配置

本次使用现有 `checkpoints/tdma_ppo_latest.pt` 断点续训，未启用同步模式：

```bash
source /home/opp_env/omnetpp-6.3.0/setenv && \
./scripts/run_joint.sh --num_slots 10 --num_nodes 9 \
  --sync_interval 0 \
  --load_ckpt checkpoints/tdma_ppo_latest.pt
```

| 项目 | 值 |
|:---|:---|
| 日志目录 | `logs/20260425_191449` |
| 仿真时长 | `t=15000s`，达到 `sim-time-limit` 正常结束 |
| 节点 / 时隙 | 9 节点 / 10 数据时隙 |
| 模式 | 异步 RL 闭环训练，`sync_interval=0` |
| 起始权重 | `checkpoints/tdma_ppo_latest.pt` |
| 输出权重 | `checkpoints/tdma_ppo_latest.pt`、`checkpoints/tdma_ppo_frame53500.pt` |
| 管道读取 | 482139 行，`_buf=0`，`pending=0` |

### 末尾训练状态

Python 训练器末尾日志：

```text
[PPO] frame=53504  update= 418  avg_r=-0.667  L_actor=+0.0002  L_critic=0.0645  entropy=0.0058  ent=0.100  lr=2.43e-04
[RLReceiver] 管道已关闭：读取 482139 行，_buf=0，pending=0，已 emit 待发帧=0
[TDMAAgent] 9 个节点权重已保存至 checkpoints/tdma_ppo_latest.pt
```

OMNeT++ 侧正常达到时间上限：

```text
<!> Simulation time limit reached -- at t=15000s, event #16672132
End.
```

### 观察

1. **训练流程稳定**：Python 管道创建、动作回传、PPO 更新、checkpoint 保存均正常；仿真进程 exit code 为 0。
2. **低熵策略已接近确定性**：末尾 `entropy=0.0058`，自适应熵系数保持 `ent=0.100`。这说明本次不是从零探索训练，而是在极低熵 checkpoint 上继续微调。
3. **逐时隙奖励接近 0 附近**：末尾 `avg_r=-0.667`，显著优于早期未收敛 baseline 的大幅负值，但仍未达到正收益。按当前 `{成功=+1, 失败=-1, 未申请=0}` 的逐时隙奖励定义，接近 0 表示冲突被大幅抑制，同时策略可能较保守。
4. **资源清理仍有提示**：仿真结束时出现多条 `undisposed object`，对象主要为 `data-phase-finalize` 和 `tx-finished` 两类 `cMessage`。这不影响本次仿真正常结束，但后续应在模块析构/finish 清理中释放相关 self-message。

### 后续建议

- 将 README 中过期的训练示例更新为当前日志格式，避免继续引用早期 `avg_r=+175`、`entropy≈0.693` 的从零训练示例。
- 若目标是提升吞吐而不是单纯避碰，需要重新审视逐时隙奖励：当前 `0` 奖励会让"不申请"成为安全动作，可能导致低碰撞但偏保守。
- 单独修复 OMNeT++ `undisposed object` 提示，避免长批量实验结束时产生资源泄漏噪声。

---

## 改进 10：长训练与 reward 修正验证（2026-04-26）

### 改动范围

本轮只推进当前主线候选 B，不改 PPO 网络结构、不复用旧 checkpoint、不继续优化 D/DB。

1. 在 `compute_per_slot_reward()` 中加入可选的轻量不作为惩罚：
   - 申请且成功：`+1`
   - 申请但失败：`-1`
   - 有队列但未申请：`-idle_queue_penalty`
   - 无队列且未申请：`0`
2. `idle_queue_penalty` 默认值为 `0.0`，旧实验和旧 checkpoint 行为保持可复现；只有显式传入 `--idle_queue_penalty 0.05` 时才启用新 reward。
3. `ppo_trainer.py`、`scripts/run_joint.sh`、`scripts/ablation.sh` 均已透传该参数。
4. `summarize_ablation_metrics.py` 的完整性判断改为基于实际 `sim_completed`、PPO frame/update、metrics 最终帧和 9 节点行，不再硬编码 15000s 的目标帧数。

### 阶段 A：baseline vs B 长训练（30000s，无 reward 修正）

结果根目录：`logs/ablation_20260425_225916`

| 组 | 完整 run | avg_r | packets/frame | tx_success | Jain 尾 100 | queue 尾 100 | slot_util |
|:---|---:|---:|---:|---:|---:|---:|---:|
| baseline | 3/3 | -4.926 ± 0.834 | 2.872 ± 0.067 | 0.7304 | 0.7559 | 1040770.2 | 0.0319 |
| B | 3/3 | -5.222 ± 1.348 | 2.948 ± 0.085 | 0.7336 | 0.7307 | 1032760.8 | 0.0328 |

**结论**：B 在 30000s 长训练下仍保持更高吞吐、更低队列、更高请求成功率和更高时隙利用率；`avg_r` 略差且公平性略低。因此 B 不是只在 15000s 中期收敛阶段有效，而是可作为当前主线候选。

### 阶段 C：启用不作为惩罚后的 baseline/B 验证

短验证 `200s` 通过，结果根目录：`logs/ablation_20260426_002012`。两组均正常生成 `slot_stats.csv`、`fairness.csv`、`frame_metrics.csv`，汇总脚本可解析。

15000s 正式验证，结果根目录：`logs/ablation_20260426_002057`

| 组 | 完整 run | avg_r | packets/frame | tx_success | Jain 尾 100 | queue 尾 100 | slot_util |
|:---|---:|---:|---:|---:|---:|---:|---:|
| baseline | 3/3 | -77.265 ± 1.538 | 5.262 ± 0.199 | 0.7173 | 0.4022 | 392469.0 | 0.0585 |
| B | 3/3 | -76.878 ± 4.186 | 5.359 ± 0.207 | 0.7141 | 0.4226 | 387283.5 | 0.0595 |

30000s 正式验证，结果根目录：`logs/ablation_20260426_010337`

| 组 | 完整 run | avg_r | packets/frame | tx_success | Jain 尾 100 | queue 尾 100 | slot_util |
|:---|---:|---:|---:|---:|---:|---:|---:|
| baseline | 3/3 | -68.715 ± 0.952 | 2.889 ± 0.155 | 0.7346 | 0.7481 | 1039369.0 | 0.0321 |
| B | 3/3 | -68.785 ± 0.958 | 2.966 ± 0.062 | 0.7348 | 0.7474 | 1030631.0 | 0.0330 |

**结论**：启用 `idle_queue_penalty=0.05` 后，`avg_r` 绝对值变大是预期结果，因为有队列但不申请现在会扣分。网络层指标仍支持 B：15000s 与 30000s 下 B 均提升吞吐、降低队列、提高时隙利用率；30000s 下请求成功率也略优。公平性差异很小，不能作为推翻 B 的依据。

### 阶段 D：B 正则系数扫描（15000s，idle_queue_penalty=0.05）

| heur_deviation_coef | 结果根目录 | 完整 run | avg_r | packets/frame | tx_success | Jain 尾 100 | queue 尾 100 | slot_util |
|---:|:---|---:|---:|---:|---:|---:|---:|---:|
| 0.0025 | `logs/ablation_20260426_022448` | 3/3 | -76.877 ± 1.451 | 5.349 ± 0.192 | 0.7145 | 0.4173 | 387210.1 | 0.0594 |
| 0.0050 | `logs/ablation_20260426_024513` | 3/3 | -77.511 ± 2.592 | 5.181 ± 0.125 | 0.7174 | 0.4490 | 396436.2 | 0.0576 |
| 0.0100 | `logs/ablation_20260426_030547` | 3/3 | -77.722 ± 1.801 | 5.428 ± 0.236 | 0.7144 | 0.4362 | 383511.9 | 0.0603 |
| 0.0200 | `logs/ablation_20260426_032608` | 3/3 | -79.376 ± 4.657 | 5.482 ± 0.324 | 0.7141 | 0.4011 | 380671.0 | 0.0609 |

**选择建议**：

- 若论文目标优先强调吞吐和队列压力，`0.02` 是当前最强配置：`packets/frame=5.482` 最高，`queue_tail100=380671.0` 最低，`slot_util=0.0609` 最高。
- 若需要兼顾公平性与方差，`0.01` 更稳健：吞吐和队列接近 `0.02`，但 `Jain_tail100=0.4362` 明显高于 `0.02`，`avg_r` 方差也更小。
- `0.005` 的请求成功率和公平性最高，但吞吐最低、队列最高，更像保守避撞配置。

**阶段性主线**：后续默认建议使用 **B + `idle_queue_penalty=0.05` + `heur_deviation_coef=0.01`**。它不是单项最优，但在吞吐、队列、公平性和训练稳定性之间更均衡；`0.02` 可作为“吞吐优先”对照组保留。

### 验证记录

- `python -m py_compile rl/rl_agent.py rl/ppo_trainer.py scripts/summarize_ablation_metrics.py` 通过。
- `bash -n scripts/run_joint.sh scripts/ablation.sh` 通过。
- 短验证：`logs/ablation_20260426_002012` 正常结束，PPO 日志和三类 metrics 文件均存在。
- 正式验证：15000s/30000s baseline/B 以及 4 个 B 系数扫描均完成；所有正式 15000s 扫描达到 `frame=53504, update=418`，仿真到达 `t=15000s`。
- metrics 校验：`slot_stats.csv`、`fairness.csv`、`frame_metrics.csv` 均非空、无 NaN/Inf；最终 `slot_stats.csv` 每个 run 均为 9 个节点行；`fairness.csv` 与 `slot_stats.csv` 最终帧一致。

---

## 改进 11：普适性验证脚手架（2026-04-26）

### 目标

验证当前主线 B 在不同节点规模和典型连接状态下是否仍优于 baseline，避免结论只绑定在 9 节点固定拓扑上。

### 实现

1. `Network.ned` 已将拓扑参数化，支持 `line`、`ring`、`star`、`grid`、`clustered`、`full` 六类典型拓扑。
2. `run_joint.sh` 和 `ablation.sh` 透传 `--topology_mode`、`--grid_cols`，并同步写入 `*.numNodes`、`**.nodes[*].numNodes`、`*.numDataSlots`、`**.nodes[*].numDataSlots`。
3. 新增 `scripts/generalization.sh`，用于生成 `节点数 × 拓扑 × 组别 × seed` 的场景矩阵；输出目录为 `logs/generalization_*/N{n}_{topology}/{group}/seed{s}/`。
4. `summarize_ablation_metrics.py` 兼容旧的 `group/seed` 目录和新的 `scenario/group/seed` 目录，并输出 `metrics_scenario_summary.csv`。

### 推荐命令

阶段一广覆盖筛选：

```bash
source /home/opp_env/omnetpp-6.3.0/setenv
./scripts/generalization.sh \
  --sim_time 5000 \
  --nodes "6 9 12 16" \
  --topologies "line ring star grid clustered full" \
  --groups "baseline B" \
  --seeds "1 2" \
  --jobs 2 \
  --metrics_mode summary \
  --save_every 5000
```

阶段二长训练复核应从阶段一结果中挑选代表场景，再用 `sim_time=15000`、`seeds="1 2 3"` 复跑。

### 验证记录

- `ablation.sh` 冒烟：`logs/ablation_20260426_110134`，`N=6`、`line`、baseline/B、`sim_time=200`，正常完成并可汇总。
- `generalization.sh` 冒烟：`logs/generalization_20260426_110210`，覆盖 `N=6/9 × line/star × baseline/B`，正常完成并生成场景汇总。
- 剩余拓扑语法验证：`logs/generalization_20260426_110302`，覆盖 `ring/grid/clustered/full`，均正常到达时间上限。

### 阶段一广覆盖筛选结果

正式筛选根目录：`logs/generalization_20260426_111420`

实验矩阵：

- 节点数：`6 9 12 16`
- 拓扑：`line ring star grid clustered full`
- 组别：`baseline B`
- seeds：`1 2`
- 仿真时长：`5000s`
- 运行模式：`jobs=2`、`metrics_mode=summary`、`save_every=5000`

完整性：

| 检查项 | 结果 |
|:---|---:|
| 计划 run 数 | 96 |
| `sim.log` 到达 `Simulation time limit reached` | 96/96 |
| `python.log` 存在 | 96/96 |
| `slot_stats.csv` 存在 | 96/96 |
| `fairness.csv` 存在 | 96/96 |
| `frame_metrics.csv` 存在 | 96/96 |
| summary 模式下 `node_features/` | 0 |

汇总产物：

- `metrics_summary.csv`
- `metrics_group_summary.csv`
- `metrics_scenario_summary.csv`

### B 相对 baseline 的网络指标胜负

按 24 个 `节点数 × 拓扑` 场景逐项比较 B 与 baseline：

| 指标 | B 优于 baseline | B 劣于 baseline | 判断 |
|:---|---:|---:|:---|
| `packets/frame` | 11/24 | 13/24 | 未形成普适吞吐优势 |
| `queue_tail100` | 10/24 | 14/24 | 未形成普适队列优势 |
| `tx_success_rate` | 11/24 | 13/24 | 成功率优势不稳定 |
| `jain_tx_tail100` | 10/24 | 14/24 | 公平性优势不稳定 |
| `avg_r` | 9/20 | 11/20 | reward 也未稳定占优；4 个 N16 场景缺完整 PPO 汇总 |

代表性正例：

| 场景 | `packets/frame` 变化 | `queue_tail100` 变化 | `jain_tx_tail100` 变化 | 解释 |
|:---|---:|---:|---:|:---|
| `N12_grid` | +0.232 | -2592.9 | -0.0181 | 吞吐和队列明显改善，公平性略降 |
| `N16_grid` | +0.078 | -818.3 | +0.0809 | 大规模网格下队列与公平性改善 |
| `N16_clustered` | +0.208 | -2626.7 | +0.0124 | 聚簇场景下 B 有一定空间复用收益 |
| `N9_ring` | -0.015 | -6.2 | +0.0209 | 轻微牺牲吞吐，换取队列和公平性改善 |

代表性反例：

| 场景 | `packets/frame` 变化 | `queue_tail100` 变化 | `jain_tx_tail100` 变化 | 解释 |
|:---|---:|---:|---:|:---|
| `N16_ring` | -0.557 | +549.5 | -0.0568 | 高规模环形拓扑下 B 明显退化 |
| `N12_star` | -0.081 | +1319.7 | -0.0478 | 中心瓶颈拓扑下 B 约束不利 |
| `N9_full` | -0.033 | +1178.1 | -0.0018 | 高耦合拓扑中无网络层收益 |
| `N9_star` | -0.031 | +541.7 | -0.0029 | 中心瓶颈场景收益不稳定 |

### 收敛性判断

本轮普适性实验应解释为“早期/中期筛选”，不能解释为“收敛性能对比”。

PPO 更新次数分布：

| 节点数 | run 数 | 有完整 PPO 记录 | update 范围 | 观察 |
|:---|---:|---:|:---|:---|
| N6 | 24 | 24 | 177/177/177 | 训练步数仍偏少，未见确定收敛 |
| N9 | 24 | 24 | 139/139/139 | 筛选可用，收敛不足 |
| N12 | 24 | 24 | 114/114/114 | 筛选可用，收敛不足 |
| N16 | 24 | 8 | 17/93/93 | 多个后补场景因清理阶段 timeout，PPO 日志不完整 |

策略熵仍接近二分类最大熵 `ln(2)=0.693`：

- 全部可用 PPO 日志的 entropy 中位数约 `0.6922`
- 说明策略大多仍接近随机探索状态，尚未形成稳定确定性调度
- N16 的网络指标 CSV 完整可用，但 PPO 收敛状态不能作为最终训练结论

### 修正结论

B 不是普适稳定优于 baseline 的最终算法。它在 `grid`、`clustered` 等存在局部空间复用机会的场景中更容易改善队列和吞吐；在 `star`、`full`、部分 `ring` 等中心瓶颈或强耦合场景中收益不稳定，甚至可能退化。

论文表述应改为：

> B 在部分具备局部空间复用结构的拓扑中改善队列压力、吞吐或公平性，但在中心瓶颈和高耦合拓扑中不具备稳定优势。当前普适性实验完成了广覆盖筛选，但尚未完成收敛性验证。

后续路线：

1. 对 `N12_grid`、`N16_grid`、`N16_clustered` 做长训练复核，确认正例是否在 `400+` 或 `800+` PPO update 后仍成立。
2. 对 `N16_ring`、`N12_star`、`N9_full` 做反例复核，判断退化来自拓扑结构、训练不足，还是 reward 不对齐。
3. 将 `heur_deviation_coef` 从固定值改为按拓扑密度或局部竞争强度自适应。
4. 继续修正 reward 中队列积压与申请动作的对齐，避免 reward 变好但吞吐/队列变差。

---

## 改进 12：按 PPO update 对齐与实时监控（2026-04-26）

### 背景

普适性筛选暴露了一个比较口径问题：不同节点数和拓扑下，同样 `sim_time=5000s` 产生的 PPO update 数不同，按仿真时间对齐会导致训练预算不公平。长实验还存在另一个工程风险：Python 或 OMNeT++ 某一端已经停止推进，但后台进程仍占用 CPU，容易形成“实验看似还在跑，实际结果不可用”的状态。

### 实现

1. `ppo_trainer.py` 增加 `--target_updates`、`--target_frames` 和 `--receiver_buffer_size`。
2. `rl_receiver.py` 修复接收缓冲：当 buffer 满时阻塞 C++ 写入，避免 OMNeT++ 快速跑完后 Python 长时间消化历史积压帧。
3. `run_joint.sh` 增加：
   - `--target_updates`
   - `--target_frames`
   - `--receiver_buffer_size`
   - `--stale_timeout`
   - `run_status.tsv` 心跳输出
4. `ablation.sh` 和 `generalization.sh` 增加 `manifest.tsv`，并在实验结束时自动调用 `monitor_runs.py --once`。
5. 新增 `scripts/monitor_runs.py`，可实时显示：

```text
scenario group seed status update frame avg_r entropy age_py age_sim speed eta
```

### 使用方式

启动按 update 对齐的实验：

```bash
./scripts/generalization.sh \
  --scenarios "N9_ring" \
  --groups "baseline B" \
  --seeds "1 2 3" \
  --target_updates 400 \
  --sim_time 20000 \
  --jobs 2 \
  --metrics_mode summary \
  --save_every 5000 \
  --stale_timeout 300
```

实时监控：

```bash
python scripts/monitor_runs.py logs/generalization_YYYYMMDD_HHMMSS --watch --interval 30
```

单次查看：

```bash
python scripts/monitor_runs.py logs/generalization_YYYYMMDD_HHMMSS --once
```

### 验证记录

- `python -m py_compile scripts/monitor_runs.py scripts/summarize_ablation_metrics.py rl/ppo_trainer.py` 通过。
- `bash -n scripts/run_joint.sh scripts/ablation.sh scripts/generalization.sh` 通过。
- `target_updates=1` 冒烟：`logs/ablation_20260426_171840`，baseline 正常到达 `update=1`，监控显示 `done=1`，汇总脚本标记 `complete=True`。
- `target_updates=2` 冒烟：`logs/ablation_20260426_171335`，baseline/B 均正常到达 `update=2`，监控显示 `done=2`。
- 发现并停止旧的 `logs/generalization_20260426_111420` 残留进程：该批次的 OMNeT++ 已结束，但 Python 仍在处理历史缓冲并占用 CPU。新监控和 target 停止机制用于避免此类问题再次发生。

### 正式 `N9_ring` 复核结果

正式结果目录：`logs/generalization_20260426_214344`

设置：

- 场景：`N9_ring`
- 组别：`baseline`、`B`
- seeds：`1 2 3`
- 对齐：`target_updates=400`
- 每个 run 达到：`ppo_update=400`、`ppo_frame=51200`

汇总：

| 组 | 完整 run | `avg_r` | `packets/frame` | `goodput/slot` | `PDR` | `tx_success` | `Jain` 尾 100 | `queue` 尾 100 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| B | 3/3 | -247.85 | 12.131 | 1.2131 | 0.9627 | 0.6937 | 0.6143 | 27.94 |
| baseline | 3/3 | -261.78 | 7.466 | 0.7466 | 0.5924 | 0.6876 | 0.6288 | 28.31 |

结论：

- B 相对 baseline 的 `goodput/slot`、`packets/frame` 和 `PDR` 均提升约 62.5%。
- B 的 `avg_r` 更好，说明本轮 reward 与网络层吞吐方向一致。
- B 的 `goodput/slot=1.2131 > 1`，说明该拓扑下存在有效空间复用。
- B 的公平性略低：`Jain_tail100=0.6143`，baseline 为 `0.6288`，论文表述中必须保留吞吐-公平性折中。

### 文献定位

结合 `docs/论文指标与性能对比基准.md` 的整理：

- B 在 `N9_ring` 上的吞吐提升幅度与 RL-BMAC 报告的吞吐提升（58.5%）属于同量级。
- 相对 Z-MAC 高竞争场景中常见的 20-40% 吞吐提升，当前本地提升幅度更高；但 Z-MAC/TRAMA 同时报告公平或能耗收益，本项目当前不能宣称真实能效优势。
- 由于尚未对比 optimal STDMA、greedy STDMA 或 oracle 上界，当前结论应限定为“相对本地 baseline 的显著网络层收益”，不能表述为“超过经典空间复用 TDMA 文献”。

### 后续工作

1. 对正例 `N12_grid`、`N16_clustered` 做 `target_updates=400` 复核。
2. 对反例 `N12_star`、`N9_full` 做同预算复核，确认退化边界。
3. 增加 greedy/traffic-adaptive STDMA 基线，补足与 optimal spatial reuse TDMA 文献的上界差距。
4. 继续改 reward 和公平正则，降低 B 的吞吐-公平性折中。

---

## 改进 13：统一 benchmark 框架入口（2026-04-26）

### 背景

后续需要在相同场景下对比不同论文方法或算法版本。直接为每个文献方法写独立脚本会导致目录、seed、指标和汇总口径不一致，不利于论文结果复查。因此先实现通用框架入口，不绑定具体文献模型。

### 实现

新增：

- `scripts/benchmark_suite.sh`：统一调度入口。
- `scripts/summarize_benchmark.py`：统一汇总 `scenario/method/seed` 结构。
- `configs/benchmark_methods.tsv`：方法注册表。

目录结构固定为：

```text
logs/benchmark_<suite>_<timestamp>/
  manifest.tsv
  suite_config.tsv
  <scenario>/
    <method>/
      seed1/
      seed2/
  summaries/
    metrics_summary.csv
    metrics_method_summary.csv
    metrics_scenario_method_summary.csv
    comparison_table.md
```

当前注册方法包括 `baseline`、`B`、`D`、`DB` 和 `external` 占位。第一版仍复用现有 `ablation.sh` runner，不实现特定文献协议。

### 使用方式

冒烟：

```bash
./scripts/benchmark_suite.sh \
  --suite smoke \
  --scenarios "N9_ring" \
  --methods "baseline B" \
  --seeds "1" \
  --target_updates 2 \
  --sim_time 300 \
  --jobs 2 \
  --metrics_mode summary
```

正式比较：

```bash
./scripts/benchmark_suite.sh \
  --suite literature_compare \
  --scenarios "N9_ring N12_grid N16_clustered N12_star N9_full" \
  --methods "baseline B" \
  --seeds "1 2 3" \
  --target_updates 400 \
  --sim_time 20000 \
  --jobs 2 \
  --metrics_mode summary \
  --save_every 5000
```

### 结论边界

该框架解决的是实验组织问题：同场景、同 seed、同指标、同目录结构。它还不等价于完成 Z-MAC、TRAMA 或 STDMA oracle 的复现。后续若要做文献级同场景公平对比，应将每个文献方法作为新的 `method` 注册并实现独立 runner。

---

## 改进 14：统一 MAC 协议实现入口与 Skill 规范（2026-04-26）

### 背景

不同文献协议可能拥有不同的时隙结构、superframe 设计和控制阶段。如果直接把每个协议临时塞进 `DynamicTDMA.cc`，后续会很难判断哪些代码属于共享仿真层，哪些属于协议差异层，也很难保证 benchmark 的 method、runner 和指标口径一致。

### 规范

后续实现其他 MAC/TDMA/论文协议时，统一使用以下层级：

```text
platform = OMNeT++
implementation = DynamicTDMA.cc / future protocol cc
macMode = 同一 implementation 内的协议模式
method = benchmark 对比方法名
runner = 启动方式
adapter = 指标归一化方式
```

`configs/benchmark_methods.tsv` 已升级为：

```text
method  implementation  network  runner  macMode  adapter  needs_rl  description
```

当前 baseline/B/D/DB 仍使用：

```text
implementation = DynamicTDMA
network = TDMANetwork
runner = ablation
macMode = dynamic_tdma
adapter = dynamic_tdma
```

### 实现原则

- 轻量协议差异优先通过 `DynamicTDMA.cc + macMode` 实现，例如 slot owner、slot stealing、优先级规则或 traffic-adaptive election。
- 如果协议改变完整 superframe、控制帧流程、sleep/listen 状态机或能耗模型，应新建独立 `.cc/.ned`，但仍接入相同 benchmark method 与 adapter 规范。
- 跨协议主比较优先使用 canonical metrics：`goodput_per_second`、`packet_delivery_ratio`、`p95_delay_s`、`jain_fairness`、`control_overhead_ratio`、`energy_proxy`。
- `packets/frame`、`goodput_per_slot` 只作为 TDMA 内部补充指标，不作为不同时隙结构协议的唯一胜负依据。

### Skill

新增本地 skill：

```text
.codex/skills/implement-mac-protocol/SKILL.md
.codex/skills/implement-mac-protocol/references/checklist.md
```

该 skill 用于后续规范化实现新协议，固定要求：

1. 先判断使用 `macMode` 还是新 `.cc/.ned`。
2. 再定义 benchmark method 注册项。
3. 实现时隔离共享仿真层和协议逻辑层。
4. 输出或转换 canonical metrics。
5. 更新 `docs/论文指标与性能对比基准.md` 和本记录。
6. 运行 smoke test 后再启动正式实验。

### 禁止事项

- 不允许只改协议逻辑但不接入 benchmark。
- 不允许用 `packets/frame` 单独判断跨协议优劣。
- 不允许把外部论文表格混称为同场景复现实验。
- 不允许提交实验日志、checkpoint、临时 ini 或 `.ablation_ini_backup_*`。

---

## 改进 15：补齐传统基线与后续仿真条件入口（2026-05-07）

### 背景

现有正式结果主要比较 `B` 与 PPO baseline，不能直接支撑“PPO-TDMA 普适优于传统 TDMA”。为了和 [研究进度与背景.md](研究进度与背景.md) 中的高动态 MANET、多节点变化、多业务 QoS 对齐，本轮先补齐可运行的传统基线入口和负载扫描入口，并把动态拓扑、节点上下线、QoS 场景固化到 benchmark 规范中。

### 代码入口

新增 `DynamicTDMA` 的 `macMode` 参数：

```text
dynamic_tdma
heuristic_only
plain_tdma
```

- `dynamic_tdma` 保持当前 PPO/启发式混合路径，用于 `baseline/ppo_baseline/B/D/DB`。
- `heuristic_only` 复用当前启发式申请概率，但不读取 PPO 动作，不启动 Python。
- `plain_tdma` 使用固定 owner slot：节点 `myId` 默认申请 `myId % numDataSlots` 对应时隙，作为传统 TDMA 基线。

`scripts/run_joint.sh` 增加 `--skip_ppo` 与 `--mac_mode`，用于 OMNeT++ only 的非学习方法；同时增加 `--traffic_rate`、`--enable_ramp_traffic`、`--enable_adaptive_traffic` 等负载参数。`scripts/benchmark_suite.sh` 新增 `omnet_only` runner，可在同一个 suite 中混跑 PPO 方法和非 PPO 方法。

### 方法注册

`configs/benchmark_methods.tsv` 增加：

```text
ppo_baseline   DynamicTDMA  TDMANetwork  ablation    dynamic_tdma    dynamic_tdma  true
plain_tdma     DynamicTDMA  TDMANetwork  omnet_only  plain_tdma      dynamic_tdma  false
heuristic_only DynamicTDMA  TDMANetwork  omnet_only  heuristic_only  dynamic_tdma  false
```

后续传统基线实验应使用：

```bash
./scripts/benchmark_suite.sh \
  --suite sim_condition_smoke \
  --scenarios "N9_ring_load_low" \
  --methods "plain_tdma heuristic_only ppo_baseline B" \
  --seeds "1" \
  --sim_time 300 \
  --jobs 2 \
  --metrics_mode summary
```

### 结论边界

本轮实现的是仿真条件入口和传统/启发式基线可运行路径；动态拓扑扰动、运行时节点 active/inactive 和多业务 QoS 的细粒度指标仍需后续继续实现。正式论文结论必须等 `plain_tdma/heuristic_only/ppo_baseline/B` 在同场景、同 seed、同业务负载下跑完后再写。

---

## 改进 16：动态拓扑与节点上下线 v1（2026-05-07）

### 背景

为了让仿真条件更贴近 [研究进度与背景.md](研究进度与背景.md) 中的高动态 MANET 和节点变化设定，本轮实现第一版运行时动态拓扑条件。工程上不直接重连 OMNeT++ gate，而是在 `DynamicTDMA` 协议层维护逻辑 active node / active edge 状态。这样可以保持 NED submodule 数量、PPO 输入维度和进程结构稳定。

### 实现范围

新增动态拓扑参数：

```text
dynamicTopologyMode = static|edge_toggle|topology_switch|node_dropout|node_rejoin
logicalTopologyMode = ""        # 为空时使用 NED 物理连接
perturbAtFrame = 1000
recoveryAtFrame = 1500
dropoutRatio = 0.25
edgeToggleRatio = 0.2
switchTopologyMode = "grid"
dynamicTopologySeed = seed-set
```

`DynamicTDMA` 维护 `activeNodes`、`baseEdges`、`activeEdges` 和 `scenarioEdges`。业务生成、RTS/CTS/DATA 广播、邻居统计、空间复用特征和接收路径均通过逻辑拓扑过滤：inactive 节点不生成业务、不申请、不发送控制帧或数据帧；收到来自 inactive 节点或 inactive edge 的消息会直接丢弃。

### 场景入口

`scripts/run_joint.sh` 与 `scripts/ablation.sh` 已透传动态参数。`scripts/benchmark_suite.sh` 增加动态场景后缀：

```text
N9_ring_to_grid
N12_grid_edge_toggle
N12_grid_node_dropout
N12_grid_node_rejoin
N16_clustered_bridge_break
```

对于需要切换到非原始邻接关系的场景，benchmark 会使用 `topologyMode=full` 作为物理承载拓扑，并用 `logicalTopologyMode` 指定初始逻辑拓扑，避免运行时无法发送到新逻辑邻居。

### 指标

新增 `metrics/topology_events.csv`，记录扰动和恢复事件。`frame_metrics.csv` 增加 `active_nodes`、`active_edges`、`dynamic_mode`、`event_phase`。汇总脚本新增扰动窗口指标：扰动前/后 PDR、PDR 下降幅度、扰动后队列峰值、扰动后 Jain 和恢复窗口 Jain。

### 结论边界

本轮只是动态条件 v1。它可以用于比较方法在离散链路扰动、拓扑切换和节点上下线下的鲁棒性，但还不等价于连续移动模型。后续若要支撑完整 MANET 移动性结论，需要继续补充坐标移动、路径损耗或连接概率模型。
